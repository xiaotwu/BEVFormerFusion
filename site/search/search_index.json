{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"BEVFormerFusion","text":"<p>Multi-modal 3D object detection fusing camera and LiDAR inputs via BEVFormer with dual encoder-decoder side fusion.</p>"},{"location":"#three-key-innovations","title":"Three Key Innovations","text":"<ul> <li>Encoder-Side Fusion -- LiDAR BEV features are injected into every encoder layer via parallel deformable cross-attention, blended with camera cross-attention through a learnable weight</li> <li>Decoder-Side Fusion -- After the encoder, LiDAR features are concatenated and projected with the BEV embedding before entering the decoder</li> <li>Velocity Head -- A dedicated cross-attention head that attends to camera-only BEV features (before LiDAR fusion) to predict object velocity</li> </ul>"},{"location":"#chapters","title":"Chapters","text":"# Chapter Topic 0 Architecture Overview End-to-end architecture, design philosophy 1 Data Pipeline nuScenes dataset, temporal queue, CAN bus 2 Camera Branch ResNet-50 + FPN feature extraction 3 LiDAR Branch PointPillars: voxelization, pillar features, BEV scatter 4 Encoder Fusion TSA, dual SCA, learnable blend weights 5 Decoder Fusion Concat + linear fusion, identity initialization 6 Transformer Decoder 6-layer decoder, reference point refinement 7 Detection Heads Classification, bbox, yaw bin/res, velocity head 8 Loss &amp; Training 5 loss functions, gradient isolation, training config 9 Inference NMS-free decoding, temporal test-time processing"},{"location":"#appendices","title":"Appendices","text":"<ul> <li>Tensor Shapes Reference</li> <li>File Map</li> </ul>"},{"location":"00-overview/","title":"Chapter 0: System Overview","text":"<p>BEVFormerFusion --- Multi-modal 3D Object Detection via Camera-LiDAR Fusion in BEV Space</p> <p>Navigation: Ch 0 -- Overview | Ch 1 -- Data Pipeline | Ch 2 -- Camera | Ch 3 -- LiDAR | Ch 4 -- Encoder Fusion | Ch 5 -- Decoder Fusion | Ch 6 -- Decoder | Ch 7 -- Heads | Ch 8 -- Loss &amp; Training | Ch 9 -- Inference | Appendix A | Appendix B</p>"},{"location":"00-overview/#1-introduction","title":"1. Introduction","text":"<p>BEVFormer is a transformer-based framework that constructs a unified Bird's-Eye-View (BEV) representation from multi-camera images using spatial cross-attention and temporal self-attention. BEVFormerFusion extends the base architecture with three targeted additions that bring LiDAR into the loop without disrupting the proven camera pipeline.</p> <p>Encoder-side fusion. Inside each BEV encoder layer, a parallel spatial cross-attention (SCA) branch queries the LiDAR BEV feature map alongside the original camera SCA branch. The two outputs are merged through a learnable blending weight, allowing the network to decide how much geometric detail to draw from each modality at every spatial location.</p> <p>Decoder-side fusion. After the encoder produces the BEV embedding, a separate copy of the LiDAR BEV features is concatenated channel-wise with the encoder output. A linear projection compresses the fused representation back to the model dimension, giving the decoder's object queries access to a combined camera-LiDAR scene summary.</p> <p>Velocity head. A dedicated prediction head estimates per-object velocity by cross-attending decoder queries to a camera-only clone of the BEV embedding --- one that has not been mixed with LiDAR. This preserves the temporal signal inherent in consecutive camera frames, which is essential for motion estimation and would be diluted by the single-frame LiDAR features.</p>"},{"location":"00-overview/#2-end-to-end-architecture","title":"2. End-to-End Architecture","text":"<p>The diagram below traces data from raw sensor inputs through feature extraction, BEV construction, fusion, decoding, and final predictions.</p> <pre><code>graph TD\n    %% \u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n    CAM[\"Camera Images&lt;br/&gt;(6 surround views)\"]\n    LID[\"LiDAR Point Cloud\"]\n\n    %% \u2500\u2500 Backbones \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n    subgraph Backbone[\"Feature Extraction\"]\n        direction TB\n        R50[\"ResNet-50 + FPN\"]\n        PP[\"PointPillars Encoder\"]\n    end\n\n    CAM --&gt; R50\n    LID --&gt; PP\n\n    R50 --&gt; MSF[\"Multi-scale&lt;br/&gt;Camera Features\"]\n    PP  --&gt; LBEV[\"LiDAR BEV&lt;br/&gt;Feature Map\"]\n\n    %% \u2500\u2500 BEV Encoder \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n    subgraph Encoder[\"BEV Encoder (4 layers)\"]\n        direction TB\n        TSA[\"Temporal Self-Attention\"]\n        SCA_CAM[\"SCA -- Camera Path\"]\n        SCA_LID[\"SCA -- LiDAR Path\"]\n        BLEND[\"Learnable Blend&lt;br/&gt;(w * cam + (1-w) * lid)\"]\n        FFN_E[\"Feed-Forward Network\"]\n\n        TSA --&gt; SCA_CAM\n        TSA --&gt; SCA_LID\n        SCA_CAM --&gt; BLEND\n        SCA_LID --&gt; BLEND\n        BLEND --&gt; FFN_E\n    end\n\n    MSF  --&gt; SCA_CAM\n    LBEV --&gt; SCA_LID\n\n    %% \u2500\u2500 BEV Split \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n    FFN_E --&gt; BEV_OUT[\"Encoder BEV Output\"]\n    BEV_OUT --&gt; CLONE_CAM[\"Clone: Camera-Only BEV\"]\n    BEV_OUT --&gt; CONCAT[\"Concat + Linear&lt;br/&gt;(Decoder-Side Fusion)\"]\n    LBEV --&gt; CONCAT\n\n    %% \u2500\u2500 Decoder \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n    subgraph Decoder[\"BEV Decoder (6 layers, 450 queries)\"]\n        direction TB\n        DEC_SA[\"Self-Attention\"]\n        DEC_CA[\"Cross-Attention&lt;br/&gt;to Fused BEV\"]\n        FFN_D[\"Feed-Forward Network\"]\n\n        DEC_SA --&gt; DEC_CA --&gt; FFN_D\n    end\n\n    CONCAT --&gt; DEC_CA\n\n    %% \u2500\u2500 Prediction Heads \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n    subgraph Heads[\"Prediction Heads\"]\n        direction LR\n        CLS[\"Classification\"]\n        BBOX[\"Bounding Box\"]\n        YAW_BIN[\"Yaw Bin\"]\n        YAW_RES[\"Yaw Residual\"]\n        VEL[\"Velocity\"]\n    end\n\n    FFN_D --&gt; CLS\n    FFN_D --&gt; BBOX\n    FFN_D --&gt; YAW_BIN\n    FFN_D --&gt; YAW_RES\n    FFN_D --&gt; VEL\n\n    %% \u2500\u2500 Velocity cross-attention to camera-only BEV \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n    CLONE_CAM --&gt;|\"cross-attn&lt;br/&gt;(temporal signal)\"| VEL\n\n    %% \u2500\u2500 Styles \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n    classDef input fill:#e8f4fd,stroke:#2196F3,stroke-width:2px,color:#0d47a1\n    classDef cam   fill:#fff3e0,stroke:#ff9800,stroke-width:2px,color:#e65100\n    classDef lid   fill:#e8f5e9,stroke:#4caf50,stroke-width:2px,color:#1b5e20\n    classDef fuse  fill:#fce4ec,stroke:#e91e63,stroke-width:2px,color:#880e4f\n    classDef head  fill:#f3e5f5,stroke:#9c27b0,stroke-width:2px,color:#4a148c\n    classDef enc   fill:#fffde7,stroke:#fbc02d,stroke-width:2px,color:#f57f17\n    classDef dec   fill:#e0f7fa,stroke:#00acc1,stroke-width:2px,color:#006064\n\n    class CAM,LID input\n    class R50,MSF cam\n    class PP,LBEV lid\n    class BLEND,CONCAT fuse\n    class CLS,BBOX,YAW_BIN,YAW_RES,VEL head\n    class TSA,SCA_CAM,SCA_LID,FFN_E enc\n    class DEC_SA,DEC_CA,FFN_D dec</code></pre>"},{"location":"00-overview/#3-three-innovations","title":"3. Three Innovations","text":""},{"location":"00-overview/#3a-encoder-side-fusion","title":"3a. Encoder-Side Fusion","text":"<p>Within each of the four encoder layers, spatial cross-attention is executed twice --- once against camera features and once against the LiDAR BEV map. A per-channel learnable weight blends the two results before they enter the feed-forward network.</p> <pre><code>graph LR\n    BEV_Q[\"BEV Queries\"]\n\n    subgraph DualSCA[\"Dual Spatial Cross-Attention\"]\n        direction TB\n        SCA_C[\"SCA: Camera Features\"]\n        SCA_L[\"SCA: LiDAR BEV\"]\n    end\n\n    BEV_Q --&gt; SCA_C\n    BEV_Q --&gt; SCA_L\n\n    SCA_C --&gt;|\"weight w\"| MIX[\"Blended Output&lt;br/&gt;w * cam + (1-w) * lid\"]\n    SCA_L --&gt;|\"weight 1-w\"| MIX\n\n    MIX --&gt; NEXT[\"Feed-Forward &amp;rarr; Next Layer\"]\n\n    classDef cam  fill:#fff3e0,stroke:#ff9800,stroke-width:2px,color:#e65100\n    classDef lid  fill:#e8f5e9,stroke:#4caf50,stroke-width:2px,color:#1b5e20\n    classDef fuse fill:#fce4ec,stroke:#e91e63,stroke-width:2px,color:#880e4f\n\n    class SCA_C cam\n    class SCA_L lid\n    class MIX fuse</code></pre>"},{"location":"00-overview/#3b-decoder-side-fusion","title":"3b. Decoder-Side Fusion","text":"<p>After the encoder, the BEV embedding is enriched with LiDAR information through a simple but effective concatenate-and-project operation. This gives every decoder query access to both modalities.</p> <pre><code>graph LR\n    ENC_BEV[\"Encoder BEV&lt;br/&gt;Output\"]\n    LID_BEV[\"LiDAR BEV&lt;br/&gt;Feature Map\"]\n\n    ENC_BEV --&gt; CAT[\"Channel-wise&lt;br/&gt;Concatenation\"]\n    LID_BEV --&gt; CAT\n\n    CAT --&gt; LIN[\"Linear Projection&lt;br/&gt;(2C &amp;rarr; C)\"]\n    LIN --&gt; DEC[\"Decoder&lt;br/&gt;Cross-Attention Input\"]\n\n    classDef cam  fill:#fff3e0,stroke:#ff9800,stroke-width:2px,color:#e65100\n    classDef lid  fill:#e8f5e9,stroke:#4caf50,stroke-width:2px,color:#1b5e20\n    classDef fuse fill:#fce4ec,stroke:#e91e63,stroke-width:2px,color:#880e4f\n    classDef dec  fill:#e0f7fa,stroke:#00acc1,stroke-width:2px,color:#006064\n\n    class ENC_BEV cam\n    class LID_BEV lid\n    class CAT,LIN fuse\n    class DEC dec</code></pre>"},{"location":"00-overview/#3c-velocity-head-with-camera-only-bev","title":"3c. Velocity Head with Camera-Only BEV","text":"<p>Velocity estimation relies on temporal cues --- the displacement of objects between consecutive frames. Because LiDAR BEV features capture only the current time step, mixing them into the velocity signal would dilute the frame-to-frame motion information carried by the camera branch. The velocity head therefore cross-attends exclusively to a camera-only clone of the BEV.</p> <pre><code>graph TD\n    ENC[\"Encoder BEV Output\"]\n\n    ENC --&gt; FUSED[\"Fused BEV&lt;br/&gt;(camera + LiDAR)\"]\n    ENC --&gt; CLONE[\"Clone: Camera-Only BEV&lt;br/&gt;(temporal signal intact)\"]\n\n    FUSED --&gt; DEC_Q[\"Decoder Queries\"]\n\n    DEC_Q --&gt; H_CLS[\"cls / bbox / yaw heads\"]\n    DEC_Q --&gt; VEL_HEAD[\"Velocity Head\"]\n    CLONE --&gt;|\"cross-attention\"| VEL_HEAD\n\n    VEL_HEAD --&gt; VX_VY[\"Predicted (vx, vy)\"]\n\n    classDef cam  fill:#fff3e0,stroke:#ff9800,stroke-width:2px,color:#e65100\n    classDef fuse fill:#fce4ec,stroke:#e91e63,stroke-width:2px,color:#880e4f\n    classDef head fill:#f3e5f5,stroke:#9c27b0,stroke-width:2px,color:#4a148c\n\n    class CLONE cam\n    class FUSED fuse\n    class VEL_HEAD,H_CLS head</code></pre>"},{"location":"00-overview/#4-design-philosophy","title":"4. Design Philosophy","text":""},{"location":"00-overview/#complementary-fusion","title":"Complementary Fusion","text":"<p>Encoder-side fusion operates at fine granularity: every BEV grid cell receives blended camera and LiDAR information through parallel cross-attention. Decoder-side fusion operates at coarse granularity: the entire BEV map is enriched with LiDAR context via concatenation before the decoder ever sees it. Together, the two stages give the model both local precision and global context from the LiDAR modality.</p>"},{"location":"00-overview/#temporal-signal-preservation","title":"Temporal Signal Preservation","text":"<p>BEVFormer's temporal self-attention aligns the current BEV with the previous frame's BEV, encoding object motion implicitly. LiDAR features, captured at a single time step, contain no such inter-frame signal. By routing the velocity head to a camera-only BEV clone, the architecture ensures that temporal displacement information remains undiluted.</p>"},{"location":"00-overview/#gradient-isolation","title":"Gradient Isolation","text":"<p>Yaw angle is decomposed into a classification bin and a regression residual, each served by its own head. Velocity gets a dedicated head with its own cross-attention layer. This separation prevents gradient conflicts between fundamentally different tasks --- directional classification, angular regression, and motion regression --- and allows each head to specialize.</p>"},{"location":"00-overview/#5-chapter-guide","title":"5. Chapter Guide","text":"Chapter Title Description 0 System Overview High-level architecture, design philosophy, and chapter roadmap (this document). 1 Data Flow End-to-end tensor journey from raw data loading through final predictions. 2 Backbone &amp; Neck ResNet-50 + FPN for cameras; PointPillars for LiDAR; feature dimensions. 3 BEV Encoder Temporal self-attention, spatial cross-attention, and BEV grid construction. 4 Encoder-Side Fusion Dual SCA branches, learnable blending weight, and training dynamics. 5 Decoder-Side Fusion Concatenation, linear projection, and how decoder queries consume fused BEV. 6 Prediction Heads Classification, bbox regression, yaw (bin + residual), and velocity heads. 7 Loss Functions Per-head losses, weighting strategy, and Hungarian matching. 8 Training Pipeline Config files, optimizer settings, augmentation, and distributed training. 9 Inference Model loading, BEV cache, post-processing, and visualization. A Tensor Shapes Reference table of key tensor dimensions at every stage. B File Map Directory structure and mapping from concepts to source files."},{"location":"00-overview/#6-architecture-reference","title":"6. Architecture Reference","text":"<p>The following image provides a consolidated view of the full fusion architecture.</p> <p></p> <p>Next: Chapter 1 -- Data Pipeline</p>"},{"location":"01-data-pipeline/","title":"Chapter 1: Data Pipeline","text":"<p>00 Overview | 01 Data Pipeline | 02 Camera | 03 LiDAR | 04 Encoder Fusion | 05 Decoder Fusion | 06 Decoder | 07 Heads | 08 Loss &amp; Training | 09 Inference | Appendix A | Appendix B</p> <p>Everything in BEVFormer begins with data. Before any attention layer fires, a carefully orchestrated pipeline loads multi-view images, LiDAR point clouds, and ego-pose metadata, then packages them into the temporal sequences the model expects. This chapter traces that journey from raw nuScenes samples to training-ready tensors.</p>"},{"location":"01-data-pipeline/#11-nuscenes-dataset","title":"1.1 nuScenes Dataset","text":"<p>BEVFormerFusion operates on the nuScenes dataset, performing 3D object detection over 10 classes:</p> # Class 0 car 1 truck 2 bus 3 trailer 4 construction_vehicle 5 pedestrian 6 motorcycle 7 bicycle 8 traffic_cone 9 barrier <p><code>CustomNuScenesDataset</code> extends mmdet3d's <code>NuScenesDataset</code>, adding camera intrinsics/extrinsics construction, CAN bus enrichment, and the temporal queuing mechanism described next.</p>"},{"location":"01-data-pipeline/#12-temporal-queue-construction","title":"1.2 Temporal Queue Construction","text":"<p>BEVFormer is a video model. Each training sample is not a single frame but a short temporal queue of 4 frames (3 history + 1 current). The dataset's <code>prepare_train_data</code> method builds this queue with a deliberate shuffle-and-drop strategy that provides temporal diversity while keeping chronological order.</p> <pre><code>flowchart TD\n    A[\"Select 4 candidate indices&lt;br/&gt;(index-3, index-2, index-1, index)\"] --&gt; B[\"Separate: 3 history + 1 current\"]\n    B --&gt; C[\"Randomly shuffle the 3 history frames\"]\n    C --&gt; D[\"Drop the first frame after shuffle\"]\n    D --&gt; E[\"Sort the remaining 2 history frames\"]\n    E --&gt; F[\"Append current frame&lt;br/&gt;(always last)\"]\n    F --&gt; G[\"Final queue: 3 frames in chronological order\"]\n\n    G --&gt; H{{\"For each frame in queue\"}}\n    H --&gt; I[\"get_data_info(i)\"]\n    I --&gt; J[\"pre_pipeline(input_dict)\"]\n    J --&gt; K[\"pipeline(input_dict)\"]\n    K --&gt; H\n\n    H --&gt; L[\"union2one(queue)&lt;br/&gt;Merge into single sample\"]</code></pre> <p>Scene boundary detection. When <code>union2one</code> iterates through the queue, it tracks <code>scene_token</code>. If the scene token changes between consecutive frames (meaning a new driving sequence has started), it:</p> <ol> <li>Sets <code>prev_bev_exists = False</code> so the model knows there is no valid prior BEV to attend to.</li> <li>Resets the ego translation and rotation deltas to zero, preventing nonsensical cross-scene motion vectors.</li> </ol> <p>When frames belong to the same scene, <code>prev_bev_exists = True</code> and relative deltas are computed normally.</p>"},{"location":"01-data-pipeline/#13-can-bus-vector","title":"1.3 CAN Bus Vector","text":"<p>Each frame carries an 18-dimensional CAN bus vector that encodes ego-vehicle pose. The vector is first populated with absolute values in <code>get_data_info</code>, then converted to relative deltas by <code>union2one</code>.</p>"},{"location":"01-data-pipeline/#layout","title":"Layout","text":"Index Content Coordinate Type (before <code>union2one</code>) After <code>union2one</code> 0:3 Ego translation (x, y, z) Absolute (ego2global) Relative delta from previous frame 3:7 Ego rotation (quaternion) Absolute (ego2global) Unchanged (absolute) 7:16 Reserved / unused Zero-filled Unchanged -2 (16) Patch angle Absolute (radians) Unchanged (absolute) -1 (17) Patch angle Absolute (degrees) Relative delta from previous frame"},{"location":"01-data-pipeline/#how-union2one-converts-absolute-to-relative","title":"How <code>union2one</code> converts absolute to relative","text":"<p>For frames within the same scene, <code>union2one</code> subtracts the previous frame's values:</p> <ul> <li><code>can_bus[:3] -= prev_pos</code> -- translation becomes a motion delta.</li> <li><code>can_bus[-1] -= prev_angle</code> -- heading angle (degrees) becomes a rotation delta.</li> </ul> <p>At scene boundaries, both are reset to zero. The quaternion at indices 3:7 and the radian angle at index -2 are kept absolute throughout; they are not differenced.</p> <p>The model's <code>PerceptionTransformer</code> later uses these deltas to shift and rotate the previous BEV embedding before temporal self-attention, aligning it with the current frame's coordinate system.</p>"},{"location":"01-data-pipeline/#14-train-pipeline","title":"1.4 Train Pipeline","text":"<p>The training pipeline applies 12 transforms in sequence. Each frame in the temporal queue passes through this pipeline independently before <code>union2one</code> merges them.</p> <pre><code>flowchart LR\n    A[\"LoadPointsFromFile\"] --&gt; B[\"LoadMultiViewImageFromFiles\"]\n    B --&gt; C[\"PhotoMetricDistortion\"]\n    C --&gt; D[\"LoadAnnotations3D\"]\n    D --&gt; E[\"ObjectRangeFilter\"]\n    E --&gt; F[\"ObjectNameFilter\"]\n    F --&gt; G[\"PointsRangeFilter\"]\n    G --&gt; H[\"NormalizeMultiviewImage\"]\n    H --&gt; I[\"RandomScaleImage&lt;br/&gt;(0.65x)\"]\n    I --&gt; J[\"PadMultiViewImage\"]\n    J --&gt; K[\"DefaultFormatBundle3D\"]\n    K --&gt; L[\"CustomCollect3D\"]</code></pre> Step Transform What It Does 1 <code>LoadPointsFromFile</code> Loads LiDAR point cloud from <code>.bin</code> file (5 dims loaded, 4 used: x, y, z, intensity). 2 <code>LoadMultiViewImageFromFiles</code> Loads 6 surround-view camera images, converts to <code>float32</code>. 3 <code>PhotoMetricDistortionMultiViewImage</code> Randomly perturbs brightness, contrast, saturation, and hue across all views (data augmentation). 4 <code>LoadAnnotations3D</code> Loads ground-truth 3D bounding boxes and class labels. 5 <code>ObjectRangeFilter</code> Removes GT boxes outside the point cloud range ([-51.2, -51.2, -5.0] to [51.2, 51.2, 3.0]). 6 <code>ObjectNameFilter</code> Keeps only objects belonging to the 10 target classes. 7 <code>PointsRangeFilter</code> Clips LiDAR points to the point cloud range. 8 <code>NormalizeMultiviewImage</code> Per-channel normalization (ImageNet mean/std) and BGR-to-RGB conversion. 9 <code>RandomScaleImageMultiViewImage</code> Resizes all camera images by a fixed scale factor of 0.65 (reduces memory). Also scales <code>lidar2img</code> matrices accordingly. 10 <code>PadMultiViewImage</code> Pads images so spatial dimensions are divisible by 32 (required by the FPN backbone). 11 <code>DefaultFormatBundle3D</code> Converts numpy arrays to PyTorch tensors and wraps them in <code>DataContainer</code>. 12 <code>CustomCollect3D</code> Selects the final output keys (<code>points</code>, <code>gt_bboxes_3d</code>, <code>gt_labels_3d</code>, <code>img</code>) and assembles <code>img_metas</code> from meta keys (lidar2img, can_bus, scene_token, etc.)."},{"location":"01-data-pipeline/#15-test-pipeline","title":"1.5 Test Pipeline","text":"<p>The test pipeline is a simplified version of the training pipeline. It omits all augmentation and GT loading.</p> <pre><code>flowchart LR\n    A[\"LoadPointsFromFile\"] --&gt; B[\"LoadMultiViewImageFromFiles\"]\n    B --&gt; C[\"NormalizeMultiviewImage\"]\n    C --&gt; D[\"MultiScaleFlipAug3D\"]\n\n    subgraph D[\"MultiScaleFlipAug3D wrapper\"]\n        E[\"RandomScaleImage (0.65x)\"] --&gt; F[\"PadMultiViewImage\"]\n        F --&gt; G[\"DefaultFormatBundle3D\"]\n        G --&gt; H[\"CustomCollect3D\"]\n    end</code></pre> <p>Key differences from training:</p> <ul> <li>No <code>PhotoMetricDistortion</code> -- no color augmentation at test time.</li> <li>No <code>LoadAnnotations3D</code> -- no ground-truth boxes needed.</li> <li>No <code>ObjectRangeFilter</code>, <code>ObjectNameFilter</code>, or <code>PointsRangeFilter</code> -- no GT filtering needed.</li> <li>Wrapped in <code>MultiScaleFlipAug3D</code> for consistent test-time formatting (single scale, no flip).</li> <li><code>CustomCollect3D</code> collects only <code>points</code> and <code>img</code> (no GT keys).</li> </ul>"},{"location":"01-data-pipeline/#16-camera-matrix-construction","title":"1.6 Camera Matrix Construction","text":"<p>For the spatial cross-attention in the encoder (Chapter 4), BEV reference points must be projected into each camera's image plane. This requires a <code>lidar2img</code> transformation matrix for each of the 6 cameras. These matrices are assembled in <code>get_data_info</code>:</p> <pre><code>lidar2cam_r   = inv(sensor2lidar_rotation)          # 3x3\nlidar2cam_t   = sensor2lidar_translation @ lidar2cam_r.T  # 1x3\nlidar2cam_rt  = [lidar2cam_r.T | -lidar2cam_t]      # 4x4 homogeneous\nlidar2img     = cam_intrinsic @ lidar2cam_rt         # 4x4\n</code></pre> <p>The resulting <code>lidar2img</code> matrix maps a 3D point in LiDAR coordinates to a 2D pixel in the camera image (in homogeneous coordinates). A list of 6 such matrices -- one per camera -- is stored in <code>img_metas['lidar2img']</code> and travels through the entire pipeline.</p> <p>When <code>RandomScaleImageMultiViewImage</code> resizes the images, it pre-multiplies a scale matrix into <code>lidar2img</code> so the projection remains consistent with the resized image dimensions.</p>"},{"location":"01-data-pipeline/#17-key-files","title":"1.7 Key Files","text":"File Path Role <code>nuscenes_dataset.py</code> <code>projects/mmdet3d_plugin/datasets/nuscenes_dataset.py</code> <code>CustomNuScenesDataset</code> with temporal queue, CAN bus, and camera matrices <code>loading.py</code> <code>projects/mmdet3d_plugin/datasets/pipelines/loading.py</code> Custom data loading transforms <code>transform_3d.py</code> <code>projects/mmdet3d_plugin/datasets/pipelines/transform_3d.py</code> Image normalization, scaling, padding, photometric distortion, <code>CustomCollect3D</code> <code>bevformer_project.py</code> <code>projects/configs/bevformer/bevformer_project.py</code> Config file defining train/test pipelines, model, and dataset settings <p>Next: Chapter 2: Camera Branch -- how multi-view images are processed through ResNet-50 and FPN.</p>"},{"location":"02-camera-branch/","title":"Chapter 2: Camera Branch","text":"<p>00 Overview | 01 Data Pipeline | 02 Camera Branch | 03 LiDAR | 04 Encoder Fusion | 05 Decoder Fusion | 06 Decoder | 07 Heads | 08 Loss &amp; Training | 09 Inference | Appendix A | Appendix B</p>"},{"location":"02-camera-branch/#overview","title":"Overview","text":"<p>The camera branch extracts visual features from 6 surround-view cameras and prepares them for the BEV encoder. It consists of two stages:</p> <ol> <li>ResNet50 backbone -- produces multi-scale feature maps at 4 resolution levels.</li> <li>FPN neck -- unifies all levels to a common 256-channel representation.</li> </ol> <p>After FPN, the features are annotated with per-camera and per-level embeddings, then flattened into a single token sequence that the encoder's deformable attention can index.</p>"},{"location":"02-camera-branch/#resnet50-backbone","title":"ResNet50 Backbone","text":"<p>Each of the 6 camera images passes through a shared ResNet50 to produce 4 feature levels at progressively lower resolution and higher channel count.</p> <pre><code>graph LR\n    A[\"6 Camera Images&lt;br/&gt;(B, 6, 3, H, W)\"] --&gt; B[\"ResNet50&lt;br/&gt;Stage 1 (frozen)\"]\n    B --&gt; C[\"Stage 2\"]\n    C --&gt; D[\"Stage 3\"]\n    D --&gt; E[\"Stage 4\"]\n\n    B --&gt;|\"256 ch, H/4\"| F1[\"C2\"]\n    C --&gt;|\"512 ch, H/8\"| F2[\"C3\"]\n    D --&gt;|\"1024 ch, H/16\"| F3[\"C4\"]\n    E --&gt;|\"2048 ch, H/32\"| F4[\"C5\"]</code></pre> <p>Key configuration:</p> Parameter Value <code>depth</code> 50 <code>num_stages</code> 4 <code>out_indices</code> (0, 1, 2, 3) <code>frozen_stages</code> 1 (Stage 1 weights are frozen) <code>lr_mult</code> 0.1 for <code>img_backbone</code> parameters <code>norm_cfg</code> BatchNorm, <code>requires_grad=False</code> <code>norm_eval</code> True (BN stays in eval mode) <code>pretrained</code> <code>torchvision://resnet50</code> (ImageNet) <p>The combination of <code>frozen_stages=1</code>, <code>lr_mult=0.1</code>, and frozen BatchNorm means the early layers are essentially fixed feature extractors, while later stages adapt slowly to the 3D detection task.</p>"},{"location":"02-camera-branch/#fpn-neck","title":"FPN Neck","text":"<p>The Feature Pyramid Network takes the 4 ResNet levels and produces 4 output levels, all with 256 channels.</p> <pre><code>graph TD\n    C2[\"C2 \u2014 256 ch\"] --&gt; FPN[\"FPN\"]\n    C3[\"C3 \u2014 512 ch\"] --&gt; FPN\n    C4[\"C4 \u2014 1024 ch\"] --&gt; FPN\n    C5[\"C5 \u2014 2048 ch\"] --&gt; FPN\n\n    FPN --&gt; P0[\"Level 0 \u2014 256 ch, H/4 x W/4\"]\n    FPN --&gt; P1[\"Level 1 \u2014 256 ch, H/8 x W/8\"]\n    FPN --&gt; P2[\"Level 2 \u2014 256 ch, H/16 x W/16\"]\n    FPN --&gt; P3[\"Level 3 \u2014 256 ch, H/32 x W/32\"]</code></pre> Parameter Value <code>in_channels</code> [256, 512, 1024, 2048] <code>out_channels</code> 256 <code>num_outs</code> 4 <code>start_level</code> 0 <code>add_extra_convs</code> <code>'on_output'</code> <code>relu_before_extra_convs</code> True <p>Each FPN output has shape <code>(B, 6, 256, H_l, W_l)</code> where <code>H_l</code> and <code>W_l</code> depend on the level.</p>"},{"location":"02-camera-branch/#feature-preparation-for-encoder","title":"Feature Preparation for Encoder","text":"<p>Before the multi-scale features enter the encoder, <code>PerceptionTransformer.get_bev_features()</code> performs several preparation steps that are easy to overlook but essential for the deformable attention to work correctly.</p>"},{"location":"02-camera-branch/#1-camera-embeddings","title":"1. Camera Embeddings","text":"<p>A learned embedding vector is added per camera, giving the network a notion of which camera a token came from:</p> <pre><code>self.cams_embeds = nn.Parameter(torch.Tensor(num_cams, embed_dims))  # (6, 256)\n# Added as: feat = feat + cams_embeds[cam_idx]\n</code></pre> <p>This is critical because the same spatial position in two different cameras corresponds to completely different 3D locations.</p>"},{"location":"02-camera-branch/#2-level-embeddings","title":"2. Level Embeddings","text":"<p>A separate learned embedding is added per FPN level, so the network can distinguish which resolution scale a token belongs to:</p> <pre><code>self.level_embeds = nn.Parameter(torch.Tensor(num_feature_levels, embed_dims))  # (4, 256)\n# Added as: feat = feat + level_embeds[lvl]\n</code></pre>"},{"location":"02-camera-branch/#3-spatial-flattening","title":"3. Spatial Flattening","text":"<p>All FPN levels are flattened into one long token sequence per camera. The spatial dimensions <code>(H_l, W_l)</code> from each level are concatenated along the token axis:</p> <pre><code>Level 0: H0*W0 tokens\nLevel 1: H1*W1 tokens\nLevel 2: H2*W2 tokens\nLevel 3: H3*W3 tokens\n\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nTotal:   sum(H_l * W_l) tokens per camera\n</code></pre> <p>The result is a single tensor of shape <code>(num_cam, B, total_tokens, 256)</code>.</p>"},{"location":"02-camera-branch/#4-spatial-bookkeeping","title":"4. Spatial Bookkeeping","text":"<p>Two index tensors are computed so that deformable attention can map back from flattened positions to 2D coordinates within each level:</p> <ul> <li><code>spatial_shapes</code> -- tensor of shape <code>(num_levels, 2)</code> storing <code>(H_l, W_l)</code> for each level.</li> <li><code>level_start_index</code> -- tensor of shape <code>(num_levels,)</code> with the cumulative start offset of each level in the flattened sequence.</li> </ul> <p>These are passed directly to <code>MSDeformableAttention3D</code> in the encoder.</p>"},{"location":"02-camera-branch/#key-files","title":"Key Files","text":"File Role <code>projects/configs/bevformer/bevformer_project.py</code> Model config: backbone, neck, and all hyperparameters <code>projects/mmdet3d_plugin/bevformer/detectors/bevformer.py</code> <code>BEVFormer.extract_img_feat()</code> -- runs backbone + neck <code>projects/mmdet3d_plugin/bevformer/modules/transformer.py</code> <code>PerceptionTransformer.get_bev_features()</code> -- feature preparation and encoder call <p>Next: Chapter 3: LiDAR Branch (PointPillars)</p>"},{"location":"03-lidar-branch/","title":"Chapter 3: LiDAR Branch (PointPillars)","text":"<p>00 Overview | 01 Data Pipeline | 02 Camera | 03 LiDAR Branch | 04 Encoder Fusion | 05 Decoder Fusion | 06 Decoder | 07 Heads | 08 Loss &amp; Training | 09 Inference | Appendix A | Appendix B</p>"},{"location":"03-lidar-branch/#overview","title":"Overview","text":"<p>The LiDAR branch converts a raw point cloud into a dense 2D BEV feature map using PointPillars. Unlike the camera branch which produces multi-scale features across 6 views, the LiDAR branch outputs a single bird's-eye-view tensor that directly aligns with the BEV grid used by the rest of the model.</p> <p>This BEV feature map is not used for standalone detection -- it exists solely to be fused with camera features in the encoder and/or decoder stages.</p>"},{"location":"03-lidar-branch/#pointpillars-pipeline","title":"PointPillars Pipeline","text":"<pre><code>graph TD\n    A[\"Raw Point Cloud&lt;br/&gt;(N, 4) \u2014 x, y, z, intensity\"] --&gt; B[\"FP32 Cast\"]\n    B --&gt; C[\"Voxelization&lt;br/&gt;(hard_voxelize CUDA kernel)\"]\n    C --&gt; D[\"PillarFeatureNet&lt;br/&gt;(PointNet-style MLP)\"]\n    D --&gt; E[\"PointPillarsScatter&lt;br/&gt;(scatter to 2D grid)\"]\n    E --&gt; F[\"BEV Feature Map&lt;br/&gt;(B, 64, 512, 512)\"]</code></pre> <p>Each step:</p> <ul> <li>FP32 Cast -- points are cast to float32 before voxelization to ensure numerical stability in the CUDA kernel.</li> <li>Voxelization -- bins raw points into vertical pillars on a regular XY grid. Each pillar holds up to 20 points.</li> <li>PillarFeatureNet -- a small PointNet applied independently to each pillar, producing a single 64-dim feature vector per pillar.</li> <li>PointPillarsScatter -- places each pillar's feature vector at the correct (x, y) location on a 512x512 grid. Empty pillars are zero-filled.</li> <li>BEV Feature Map -- the final output is a dense tensor of shape <code>(B, 64, 512, 512)</code> covering the full point cloud range.</li> </ul>"},{"location":"03-lidar-branch/#voxelization-detail","title":"Voxelization Detail","text":"<p>The voxelization step uses mmdet3d's <code>hard_voxelize</code> CUDA kernel (invoked via <code>Voxelization</code> in <code>mmcv.ops</code>). It assigns each point to a pillar based on its XY coordinates and clips to a maximum number of points per pillar.</p> Parameter Value Notes <code>voxel_size</code> [0.2, 0.2, 8] 0.2m XY resolution; single Z bin spans full height <code>point_cloud_range</code> [-51.2, -51.2, -5.0, 51.2, 51.2, 3.0] 102.4m x 102.4m coverage <code>max_num_points</code> 20 Points per pillar (excess discarded) <code>max_voxels</code> (30000, 40000) (train, test) limits on total active pillars <p>With <code>voxel_size=[0.2, 0.2, 8]</code> and a range of 102.4m in each horizontal axis, the grid is <code>102.4 / 0.2 = 512</code> cells in both X and Y. The Z voxel size of 8m means there is effectively one vertical bin -- hence \"pillars\" rather than full 3D voxels.</p> <p>Outputs of voxelization: - voxels -- <code>(num_pillars, max_points, 4)</code> padded point coordinates per pillar - num_points -- <code>(num_pillars,)</code> how many valid points in each pillar - coors -- <code>(num_pillars, 3)</code> the (batch_idx, y_idx, x_idx) grid coordinate of each pillar</p>"},{"location":"03-lidar-branch/#pillarfeaturenet","title":"PillarFeatureNet","text":"<p>PillarFeatureNet applies a PointNet-style operation to each pillar independently:</p> <ol> <li>Augmentation -- each point's features are augmented with offsets from the pillar center (x - x_c, y - y_c, z - z_c) and optionally the pillar center coordinates themselves.</li> <li>Linear + BN + ReLU -- a single-layer MLP maps the augmented features to 64 dimensions.</li> <li>Max pooling -- a max operation over all valid points in each pillar produces a single 64-dim vector.</li> </ol> Parameter Value <code>in_channels</code> 4 (x, y, z, intensity) <code>feat_channels</code> [64] <code>with_distance</code> False <code>norm_cfg</code> BN1d, eps=1e-3, momentum=0.01 <p>The output is a sparse set of pillar features: <code>(num_pillars, 64)</code>.</p>"},{"location":"03-lidar-branch/#pointpillarsscatter","title":"PointPillarsScatter","text":"<p>The scatter module converts the sparse pillar features into a dense 2D pseudo-image. For each pillar, its 64-dim feature vector is placed at the grid location given by <code>coors</code>:</p> <pre><code>dense_map[batch, :, y_idx, x_idx] = pillar_feature\n</code></pre> <p>Locations with no points remain zero. The output shape is <code>(B, 64, 512, 512)</code>.</p> Parameter Value <code>in_channels</code> 64 <code>output_shape</code> (512, 512)"},{"location":"03-lidar-branch/#bev-token-projection","title":"BEV Token Projection","text":"<p>The raw PointPillars output <code>(B, 64, 512, 512)</code> cannot be directly fused with camera BEV features, which are 256-dimensional and live on a 100x100 grid. A projection step bridges this gap. The projection is applied in <code>PerceptionTransformer</code> and follows the same pattern for both encoder-side and decoder-side fusion:</p> <pre><code>graph LR\n    A[\"PointPillars BEV&lt;br/&gt;(B, 64, 512, 512)\"] --&gt; B[\"Bilinear Interpolate&lt;br/&gt;512 -&gt; 100\"]\n    B --&gt; C[\"Conv2d(64, 256, 1)&lt;br/&gt;channel projection\"]\n    C --&gt; D[\"Flatten + Permute&lt;br/&gt;(B, 10000, 256)\"]</code></pre>"},{"location":"03-lidar-branch/#decoder-side-projection","title":"Decoder-side projection","text":"<p>For decoder-side fusion, the projected tokens pass through an additional normalization and scaling step before being concatenated with camera BEV tokens:</p> <pre><code>bev_lidar_tok = F.layer_norm(bev_lidar_tok, (256,))\nbev_lidar_tok = bev_lidar_tok * 5.0\n</code></pre>"},{"location":"03-lidar-branch/#the-magnitude-matching-problem","title":"The magnitude matching problem","text":"<p>This normalization exists to solve a practical problem: after <code>Conv2d</code> projection, the LiDAR token magnitudes are typically much smaller than the camera BEV embeddings produced by the encoder. Without correction, the LiDAR signal would be drowned out during the concatenation-based fusion. The <code>layer_norm</code> + <code>* 5.0</code> scaling brings LiDAR tokens to roughly the same magnitude as camera tokens, allowing the downstream <code>lidar_fuse_linear</code> layer to meaningfully combine both modalities.</p> <p>The encoder-side path does not use this scaling because it fuses via deformable cross-attention rather than concatenation -- the attention mechanism naturally handles differing magnitudes through its learned weights.</p>"},{"location":"03-lidar-branch/#projection-modules-by-fusion-mode","title":"Projection modules by fusion mode","text":"Fusion Mode Projection Layer Extra Normalization Encoder-side <code>lidar_encoder_proj</code> (Conv2d 64-&gt;256) None Decoder-side <code>lidar_proj</code> (Conv2d 64-&gt;256) LayerNorm + scale by 5.0 Both (<code>encoder_decoder</code>) Separate projection for each path Decoder path only"},{"location":"03-lidar-branch/#configuration-table","title":"Configuration Table","text":"<p>All PointPillars parameters as configured in <code>bevformer_project.py</code>:</p> Parameter Config Key Value Voxel size <code>pts_voxel_layer.voxel_size</code> [0.2, 0.2, 8] Point cloud range <code>pts_voxel_layer.point_cloud_range</code> [-51.2, -51.2, -5.0, 51.2, 51.2, 3.0] Max points per pillar <code>pts_voxel_layer.max_num_points</code> 20 Max voxels (train/test) <code>pts_voxel_layer.max_voxels</code> (30000, 40000) Input channels <code>pts_voxel_encoder.in_channels</code> 4 (x, y, z, intensity) Feature channels <code>pts_voxel_encoder.feat_channels</code> [64] Distance feature <code>pts_voxel_encoder.with_distance</code> False Encoder norm <code>pts_voxel_encoder.norm_cfg</code> BN1d, eps=1e-3, momentum=0.01 Scatter input channels <code>pts_middle_encoder.in_channels</code> 64 Scatter output shape <code>pts_middle_encoder.output_shape</code> (512, 512) Grid size (train_cfg) <code>train_cfg.pts.grid_size</code> [512, 512, 1]"},{"location":"03-lidar-branch/#key-files","title":"Key Files","text":"File Role <code>projects/configs/bevformer/bevformer_project.py</code> PointPillars configuration (<code>pts_voxel_layer</code>, <code>pts_voxel_encoder</code>, <code>pts_middle_encoder</code>) <code>projects/mmdet3d_plugin/bevformer/detectors/bevformer.py</code> <code>BEVFormer.extract_pts_feat()</code> -- runs the full PointPillars pipeline <code>projects/mmdet3d_plugin/bevformer/modules/transformer.py</code> <code>PerceptionTransformer</code> -- contains <code>lidar_proj</code>, <code>lidar_encoder_proj</code>, and the magnitude scaling logic <code>mmdet3d/models/voxel_encoders/pillar_encoder.py</code> <code>PillarFeatureNet</code> implementation <code>mmdet3d/models/middle_encoders/pillar_scatter.py</code> <code>PointPillarsScatter</code> implementation <p>Next: Chapter 4: Encoder-Side Fusion</p>"},{"location":"04-encoder-fusion/","title":"Chapter 4: Encoder-Side Fusion","text":"<p>The heart of BEVFormerFusion --- where camera and LiDAR information merge inside the BEV encoder through dual spatial cross-attention and learnable blending.</p> <p>Navigation: Ch 0 -- Overview | Ch 1 -- Data Pipeline | Ch 2 -- Camera | Ch 3 -- LiDAR | Ch 4 -- Encoder Fusion | Ch 5 -- Decoder Fusion | Ch 6 -- Decoder | Ch 7 -- Heads | Ch 8 -- Loss &amp; Training | Ch 9 -- Inference | Appendix A | Appendix B</p>"},{"location":"04-encoder-fusion/#1-overview","title":"1. Overview","text":"<p>The BEV encoder consists of 4 stacked layers, each refining a shared set of BEV queries. In the fusion-enabled configuration, each layer is an instance of <code>MM_BEVFormerLayer</code> (as opposed to the camera-only <code>BEVFormerLayer</code>). The operation order within every layer is:</p> <pre><code>self_attn -&gt; norm -&gt; cross_attn -&gt; norm -&gt; ffn -&gt; norm\n</code></pre> <p>which maps to:</p> <ol> <li>Temporal Self-Attention (TSA) --- align current BEV queries with the previous frame.</li> <li>LayerNorm</li> <li>Dual Spatial Cross-Attention --- camera SCA and LiDAR SCA run in parallel, then blend.</li> <li>LayerNorm</li> <li>Feed-Forward Network (FFN)</li> <li>LayerNorm</li> </ol> <p>The key difference from vanilla BEVFormer is step 3: instead of a single camera SCA, <code>MM_BEVFormerLayer</code> runs two cross-attention branches and merges their outputs with a learnable scalar weight.</p>"},{"location":"04-encoder-fusion/#2-encoder-layer-architecture","title":"2. Encoder Layer Architecture","text":"<p>The following diagram shows one complete <code>MM_BEVFormerLayer</code>. Subgraphs group the three major stages.</p> <pre><code>graph TD\n    classDef cam fill:#4a90d9,stroke:#2c5f8a,color:#fff\n    classDef lidar fill:#6ab04c,stroke:#3d7a2a,color:#fff\n    classDef blend fill:#f0932b,stroke:#c06f1a,color:#fff\n    classDef norm fill:#dfe6e9,stroke:#b2bec3,color:#333\n    classDef ffn fill:#a29bfe,stroke:#6c5ce7,color:#fff\n\n    INPUT[\"BEV Query&lt;br/&gt;(B, HW, C)\"] --&gt; TSA_SUB\n\n    subgraph TSA_SUB[\"Temporal Self-Attention\"]\n        direction TB\n        PREV[\"prev_bev&lt;br/&gt;(frozen from last frame)\"]\n        CUR[\"current query\"]\n        INPUT2[\"query\"] --&gt; CUR\n        CUR &amp; PREV --&gt; CAT[\"Concatenate along channel&lt;br/&gt;for offset &amp; weight prediction\"]\n        CUR &amp; PREV --&gt; STACK[\"Stack as deformable&lt;br/&gt;attention value (2 frames)\"]\n        CAT --&gt; OFFSETS[\"Predict sampling offsets&lt;br/&gt;&amp; attention weights\"]\n        OFFSETS --&gt; DEFORM_TSA[\"Deformable Attention&lt;br/&gt;on stacked BEV frames\"]\n        STACK --&gt; DEFORM_TSA\n        DEFORM_TSA --&gt; TMEAN[\"Temporal mean&lt;br/&gt;over 2 frames\"]\n        TMEAN --&gt; RESID_TSA[\"+ residual\"]\n    end\n\n    RESID_TSA --&gt; NORM1[\"LayerNorm\"]:::norm\n\n    NORM1 --&gt; SCA_SUB\n\n    subgraph SCA_SUB[\"Dual Spatial Cross-Attention\"]\n        direction TB\n        Q_IN[\"query\"] --&gt; CAM_PATH &amp; LIDAR_PATH\n\n        subgraph CAM_PATH[\"Camera SCA\"]\n            direction TB\n            PROJ[\"Project BEV ref points&lt;br/&gt;to image coords via lidar2img\"]\n            VIS[\"Visibility masking&lt;br/&gt;(which queries seen by which cameras)\"]\n            REBATCH[\"Sparse re-batching&lt;br/&gt;(only visible queries per camera)\"]\n            MSDA[\"MSDeformableAttention3D&lt;br/&gt;pillar heights + deformable offsets&lt;br/&gt;+ multi-level FPN sampling\"]\n            SCATTER[\"Scatter-add back to&lt;br/&gt;BEV grid, average over cameras\"]\n            OUT_PROJ_CAM[\"Output projection + residual\"]\n            PROJ --&gt; VIS --&gt; REBATCH --&gt; MSDA --&gt; SCATTER --&gt; OUT_PROJ_CAM\n        end\n\n        subgraph LIDAR_PATH[\"LiDAR SCA\"]\n            direction TB\n            LREF[\"BEV grid reference points\"]\n            LDEFORM[\"CustomMSDeformableAttention&lt;br/&gt;on LiDAR BEV tokens&lt;br/&gt;(1 level, 4 points, 8 heads)\"]\n            LREF --&gt; LDEFORM\n        end\n\n        OUT_PROJ_CAM --&gt; BLEND[\"Learnable Blend&lt;br/&gt;cam * w + lidar * (1-w)\"]\n        LDEFORM --&gt; BLEND\n    end\n\n    CAM_PATH:::cam\n    LIDAR_PATH:::lidar\n    BLEND:::blend\n\n    BLEND --&gt; NORM2[\"LayerNorm\"]:::norm\n    NORM2 --&gt; FFN_BLOCK[\"FFN&lt;br/&gt;(Linear -&gt; ReLU -&gt; Linear)\"]:::ffn\n    FFN_BLOCK --&gt; NORM3[\"LayerNorm\"]:::norm\n    NORM3 --&gt; OUTPUT[\"Output to next layer&lt;br/&gt;(B, HW, C)\"]</code></pre>"},{"location":"04-encoder-fusion/#3-temporal-self-attention-tsa","title":"3. Temporal Self-Attention (TSA)","text":"<p>TSA allows BEV queries to look at where objects were in the previous frame, enabling motion-aware features. The first frame of a scene has no history and skips TSA entirely (the query passes through unchanged).</p> <pre><code>graph TD\n    classDef input fill:#74b9ff,stroke:#0984e3,color:#333\n    classDef process fill:#dfe6e9,stroke:#b2bec3,color:#333\n    classDef output fill:#55efc4,stroke:#00b894,color:#333\n\n    CUR_BEV[\"Current BEV Query&lt;br/&gt;(B, HW, C)\"]:::input\n    PREV_BEV[\"Previous BEV&lt;br/&gt;(B, HW, C) -- detached\"]:::input\n\n    CUR_BEV &amp; PREV_BEV --&gt; CONCAT[\"Concatenate along channel dim&lt;br/&gt;-&gt; (B, HW, 2C)\"]:::process\n\n    CONCAT --&gt; OFFSETS[\"sampling_offsets Linear(2C -&gt; ...)&lt;br/&gt;Predict where to sample on BEV grid\"]:::process\n    CONCAT --&gt; WEIGHTS[\"attention_weights Linear(2C -&gt; ...)&lt;br/&gt;Predict per-point attention weights\"]:::process\n\n    CUR_BEV &amp; PREV_BEV --&gt; VALUE_STACK[\"Stack as value&lt;br/&gt;-&gt; (B*2, HW, C)\"]:::process\n\n    VALUE_STACK --&gt; VPROJ[\"value_proj Linear(C -&gt; C)\"]:::process\n\n    OFFSETS &amp; WEIGHTS &amp; VPROJ --&gt; DEFORM[\"MS Deformable Attention&lt;br/&gt;spatial_shapes = [[H, W]]&lt;br/&gt;Samples both frames independently\"]:::process\n\n    DEFORM --&gt; FUSE[\"Temporal mean over 2 frames&lt;br/&gt;(B*2, HW, C) -&gt; (B, HW, C)\"]:::process\n\n    FUSE --&gt; OUT_PROJ[\"output_proj Linear(C -&gt; C)\"]:::process\n\n    OUT_PROJ --&gt; RESIDUAL[\"+ identity (residual)\"]:::output\n\n    SKIP[\"First frame: no prev_bev&lt;br/&gt;-&gt; skip TSA, return identity\"] -.-&gt; RESIDUAL</code></pre> <p>Key design insight: The offset and weight prediction networks take <code>2C</code>-dimensional input (the concatenation of current and previous BEV features). This allows the network to reason about temporal differences --- if an object moved between frames, the difference in features at that location tells the network where to look in the previous frame.</p>"},{"location":"04-encoder-fusion/#4-camera-spatial-cross-attention","title":"4. Camera Spatial Cross-Attention","text":"<p>Camera SCA is the mechanism that lifts 2D image features into the 3D BEV grid. Each BEV query learns to attend to relevant image regions across all cameras.</p> <pre><code>graph TD\n    classDef cam fill:#4a90d9,stroke:#2c5f8a,color:#fff\n    classDef process fill:#dfe6e9,stroke:#b2bec3,color:#333\n    classDef output fill:#55efc4,stroke:#00b894,color:#333\n\n    BEV_Q[\"BEV Query (B, HW, C)\"]:::cam\n    REF3D[\"3D Reference Points&lt;br/&gt;(4 pillar heights per BEV cell)\"]\n\n    REF3D --&gt; POINT_SAMPLE[\"point_sampling:&lt;br/&gt;multiply by lidar2img (4x4)&lt;br/&gt;-&gt; project to each camera's image plane\"]:::process\n\n    POINT_SAMPLE --&gt; REF_CAM[\"reference_points_cam&lt;br/&gt;(B, HW, N_cam, 4, 2)&lt;br/&gt;normalized image coords\"]:::process\n    POINT_SAMPLE --&gt; MASK[\"bev_mask&lt;br/&gt;(B, N_cam, HW)&lt;br/&gt;which queries are visible per camera\"]:::process\n\n    MASK --&gt; SPARSE[\"Sparse re-batching:&lt;br/&gt;select only visible queries per camera&lt;br/&gt;(avoids wasting compute on occluded cells)\"]:::process\n    BEV_Q --&gt; SPARSE\n    REF_CAM --&gt; SPARSE\n\n    SPARSE --&gt; QUERIES_PER_CAM[\"Per-camera query subsets&lt;br/&gt;(B * N_cam, max_len, C)\"]:::process\n\n    FPN[\"Multi-level FPN features&lt;br/&gt;(per camera, per level)\"]:::cam\n\n    QUERIES_PER_CAM &amp; FPN --&gt; MSDA3D[\"MSDeformableAttention3D:&lt;br/&gt;4 pillar-height anchors per query&lt;br/&gt;x num_points deformable offsets&lt;br/&gt;x 4 FPN levels&lt;br/&gt;= dense multi-scale sampling\"]:::process\n\n    MSDA3D --&gt; SCATTER[\"Scatter-add results&lt;br/&gt;back to full BEV grid\"]:::process\n\n    SCATTER --&gt; AVG[\"Average over cameras&lt;br/&gt;that see each query\"]:::process\n\n    AVG --&gt; OUTPROJ[\"Output projection&lt;br/&gt;Linear(C -&gt; C)\"]:::process\n\n    OUTPROJ --&gt; RESID[\"+ residual (input query)\"]:::output</code></pre> <p>The sparse re-batching step is critical for efficiency: a rear-facing camera sees at most ~25% of the BEV grid, so processing only visible queries per camera avoids significant wasted computation.</p> <p>MSDeformableAttention3D operates with <code>num_Z_anchors</code> (default 4) reference points per query, stacked vertically at different heights along the pillar. For each anchor, the network predicts <code>num_points</code> offsets across <code>num_levels</code> FPN levels. The total sampling budget per query per camera is <code>4 heights * num_points * 4 levels</code>.</p>"},{"location":"04-encoder-fusion/#5-lidar-spatial-cross-attention","title":"5. LiDAR Spatial Cross-Attention","text":"<p>The LiDAR SCA branch is intentionally simple. Since LiDAR BEV tokens already live in the same BEV coordinate frame as the queries, no 3D-to-2D projection is needed.</p> <pre><code>graph TD\n    classDef lidar fill:#6ab04c,stroke:#3d7a2a,color:#fff\n    classDef process fill:#dfe6e9,stroke:#b2bec3,color:#333\n    classDef output fill:#55efc4,stroke:#00b894,color:#333\n\n    BEV_Q[\"BEV Query&lt;br/&gt;(B, HW, C)\"]:::lidar\n    LIDAR_TOK[\"LiDAR BEV Tokens&lt;br/&gt;(B, HW, C)&lt;br/&gt;pre-projected by lidar_encoder_proj\"]:::lidar\n    REF2D[\"BEV Grid Reference Points&lt;br/&gt;(B, HW, 1, 2)\"]\n\n    BEV_Q &amp; LIDAR_TOK &amp; REF2D --&gt; DEFORM[\"CustomMSDeformableAttention&lt;br/&gt;1 BEV level | 4 deformable points | 8 heads\"]:::process\n\n    DEFORM --&gt; OUT[\"LiDAR SCA output&lt;br/&gt;(B, HW, C)\"]:::output</code></pre> <p>Configuration: - 1 spatial level (flat BEV grid, no multi-scale) - 4 deformable sampling points per head - 8 attention heads - Uses <code>ref_2d</code> (the same 2D BEV grid coordinates used by TSA)</p> <p>The LiDAR BEV tokens are produced upstream in <code>PerceptionTransformer.get_bev_features</code> by a <code>1x1 Conv2d</code> (<code>lidar_encoder_proj</code>) that maps the raw LiDAR BEV feature map (64 channels from the PointPillars backbone) to the model dimension (256), followed by spatial interpolation to match the BEV grid resolution.</p>"},{"location":"04-encoder-fusion/#6-learnable-blend-weight","title":"6. Learnable Blend Weight","text":"<p>The outputs of camera SCA and LiDAR SCA are fused with a single learnable scalar per layer:</p> <pre><code>graph LR\n    classDef cam fill:#4a90d9,stroke:#2c5f8a,color:#fff\n    classDef lidar fill:#6ab04c,stroke:#3d7a2a,color:#fff\n    classDef blend fill:#f0932b,stroke:#c06f1a,color:#fff\n\n    CAM_OUT[\"Camera SCA output\"]:::cam\n    LIDAR_OUT[\"LiDAR SCA output\"]:::lidar\n\n    CAM_OUT --&gt; MUL_C[\"* w\"]\n    LIDAR_OUT --&gt; MUL_L[\"* (1 - w)\"]\n\n    LOGIT[\"cross_model_weights_logit&lt;br/&gt;nn.Parameter(scalar)&lt;br/&gt;init = 0.0\"] --&gt; SIGMOID[\"sigmoid -&gt; w\"]:::blend\n    SIGMOID --&gt; MUL_C\n    SIGMOID --&gt; MUL_L\n\n    MUL_C --&gt; ADD[\"query = cam*w + lidar*(1-w)\"]:::blend\n    MUL_L --&gt; ADD</code></pre> <p>Implementation detail (from <code>MM_BEVFormerLayer.__init__</code>):</p> <pre><code>self.cross_model_weights_logit = nn.Parameter(torch.tensor(0.0), requires_grad=True)\n</code></pre> <p>At initialization, <code>sigmoid(0.0) = 0.5</code>, so both modalities contribute equally. During training, each of the 4 encoder layers learns its own weight independently.</p> <p>Design rationale: This is a minimum-parameter approach to modality blending --- just one scalar per layer (4 total for the encoder). It avoids the risk of overfitting that comes with more complex gating mechanisms while still letting the network learn the optimal balance between camera texture and LiDAR geometry at each depth of the encoder. If the LiDAR branch provides no value for a particular layer, the weight can shift toward <code>w = 1.0</code> (camera-only); conversely, layers that benefit from geometric precision can increase the LiDAR contribution.</p> <p>When <code>lidar_bev_tokens</code> is <code>None</code> (camera-only inference), the LiDAR branch is skipped entirely and the camera output is used directly without any blending.</p>"},{"location":"04-encoder-fusion/#7-bev-reference-points","title":"7. BEV Reference Points","text":"<p>The encoder uses two types of reference points, generated once and shared across all 4 layers:</p>"},{"location":"04-encoder-fusion/#ref_3d-pillar-heights-for-camera-sca","title":"ref_3d --- Pillar heights for camera SCA","text":"<p>Generated by <code>BEVFormerEncoder.get_reference_points(dim='3d')</code>: - For each BEV cell, create <code>num_points_in_pillar</code> (default 4) points spaced evenly along the Z axis. - Each point is in normalized <code>[0, 1]</code> coordinates within the configured <code>pc_range</code>. - These are then projected to image coordinates via <code>point_sampling</code>, which applies the <code>lidar2img</code> (4x4) transformation matrices to map each 3D point onto each camera's image plane. - The resulting <code>reference_points_cam</code> and <code>bev_mask</code> tell camera SCA where to look and which queries are visible.</p>"},{"location":"04-encoder-fusion/#ref_2d-flat-bev-grid-for-tsa-and-lidar-sca","title":"ref_2d --- Flat BEV grid for TSA and LiDAR SCA","text":"<p>Generated by <code>BEVFormerEncoder.get_reference_points(dim='2d')</code>: - A simple normalized meshgrid over the BEV plane: <code>(x, y)</code> in <code>[0, 1]</code>. - Used as reference points for both Temporal Self-Attention (sampling locations on the BEV grid) and LiDAR SCA (deformable attention anchors).</p>"},{"location":"04-encoder-fusion/#point_sampling-3d-to-image-projection","title":"point_sampling --- 3D to image projection","text":"<p>The <code>point_sampling</code> method: 1. Scales normalized reference points back to metric coordinates using <code>pc_range</code>. 2. Appends a homogeneous coordinate (<code>1</code>). 3. Multiplies by each camera's <code>lidar2img</code> matrix to get pixel coordinates. 4. Normalizes by depth (perspective divide). 5. Creates <code>bev_mask</code> by checking which projected points fall within image bounds and have positive depth.</p>"},{"location":"04-encoder-fusion/#8-4-layer-encoder-loop","title":"8. 4-Layer Encoder Loop","text":"<p>The encoder runs the same sequence of operations 4 times. The BEV query is refined progressively, while <code>prev_bev</code> remains frozen throughout.</p> <pre><code>graph LR\n    classDef layer fill:#a29bfe,stroke:#6c5ce7,color:#fff\n    classDef data fill:#dfe6e9,stroke:#b2bec3,color:#333\n\n    INIT[\"Initial BEV Query&lt;br/&gt;(from learnable embedding&lt;br/&gt;+ CAN bus)\"]:::data\n    PREV[\"prev_bev&lt;br/&gt;(frozen, same for all layers)\"]:::data\n\n    INIT --&gt; L0[\"MM_BEVFormerLayer 0\"]:::layer\n    L0 --&gt; L1[\"MM_BEVFormerLayer 1\"]:::layer\n    L1 --&gt; L2[\"MM_BEVFormerLayer 2\"]:::layer\n    L2 --&gt; L3[\"MM_BEVFormerLayer 3\"]:::layer\n    L3 --&gt; OUT[\"Encoder Output&lt;br/&gt;(B, HW, C)\"]:::data\n\n    PREV -.-&gt; L0\n    PREV -.-&gt; L1\n    PREV -.-&gt; L2\n    PREV -.-&gt; L3\n\n    CAM[\"Camera FPN Features\"]:::data -.-&gt; L0 &amp; L1 &amp; L2 &amp; L3\n    LID[\"LiDAR BEV Tokens\"]:::data -.-&gt; L0 &amp; L1 &amp; L2 &amp; L3</code></pre> <p>Key points: - The query output of each layer becomes the query input of the next layer. - <code>prev_bev</code> is the encoder output from the previous timestep, detached from the computation graph (no gradient flows back in time). - Camera FPN features and LiDAR BEV tokens are shared across all layers (computed once upstream). - Reference points (<code>ref_2d</code>, <code>ref_3d</code>, <code>reference_points_cam</code>, <code>bev_mask</code>) are also computed once and reused.</p>"},{"location":"04-encoder-fusion/#9-key-files","title":"9. Key Files","text":"File Class / Function Role <code>projects/.../modules/encoder.py</code> <code>BEVFormerEncoder</code> Encoder loop: generates reference points, runs 4 layers <code>projects/.../modules/encoder.py</code> <code>MM_BEVFormerLayer</code> Fusion-enabled encoder layer with dual SCA + blend <code>projects/.../modules/encoder.py</code> <code>BEVFormerLayer</code> Camera-only encoder layer (no LiDAR branch) <code>projects/.../modules/spatial_cross_attention.py</code> <code>SpatialCrossAttention</code> Camera SCA: projection, sparse rebatch, scatter-add <code>projects/.../modules/spatial_cross_attention.py</code> <code>MSDeformableAttention3D</code> Inner deformable attention with pillar-height anchors <code>projects/.../modules/temporal_self_attention.py</code> <code>TemporalSelfAttention</code> TSA: temporal deformable attention over BEV frame pairs <code>projects/.../modules/transformer.py</code> <code>PerceptionTransformer</code> Orchestrates encoder + decoder; prepares LiDAR tokens <code>projects/.../modules/decoder.py</code> <code>CustomMSDeformableAttention</code> Deformable attention used by LiDAR SCA branch <p>All paths are relative to the repository root under <code>projects/mmdet3d_plugin/bevformer/</code>.</p> <p>Navigation: Ch 0 -- Overview | Ch 1 -- Data Pipeline | Ch 2 -- Camera | Ch 3 -- LiDAR | Ch 4 -- Encoder Fusion | Ch 5 -- Decoder Fusion | Ch 6 -- Decoder | Ch 7 -- Heads | Ch 8 -- Loss &amp; Training | Ch 9 -- Inference | Appendix A | Appendix B</p>"},{"location":"05-decoder-fusion/","title":"Chapter 5: Decoder-Side Fusion","text":"<p>Navigation: Ch 0 -- Overview | Ch 1 -- Data Pipeline | Ch 2 -- Camera | Ch 3 -- LiDAR | Ch 4 -- Encoder Fusion | Ch 5 -- Decoder Fusion | Ch 6 -- Decoder | Ch 7 -- Heads | Ch 8 -- Loss &amp; Training | Ch 9 -- Inference | Appendix A | Appendix B</p>"},{"location":"05-decoder-fusion/#1-overview","title":"1. Overview","text":"<p>After the BEV encoder produces its output, a second LiDAR injection point enriches the representation before it reaches the decoder. Where encoder-side fusion (Chapter 4) blends LiDAR into BEV queries at every encoder layer through dual spatial cross-attention, decoder-side fusion performs a single concat + linear projection operation on the fully-formed encoder output.</p> <p>The two fusion stages are complementary. Encoder-side fusion provides fine-grained, per-layer refinement. Decoder-side fusion provides a direct LiDAR signal that bypasses the encoder entirely, ensuring that no LiDAR information is lost to iterative processing. When <code>fusion_mode='encoder_decoder'</code>, both stages are active simultaneously.</p>"},{"location":"05-decoder-fusion/#2-pipeline-diagram","title":"2. Pipeline Diagram","text":"<p>The following diagram traces the full decoder-side fusion pipeline, from encoder output through to the fused BEV that enters the decoder.</p> <pre><code>graph TD\n    ENC_OUT[\"Encoder Output&lt;br/&gt;(bev_embed)\"]\n\n    subgraph CamPreserve[\"Camera-Only BEV Preservation\"]\n        direction TB\n        CLONE[\"bev_embed.clone()\"]\n        CAM_BEV[\"bev_embed_cam&lt;br/&gt;(saved for velocity head)\"]\n        CLONE --&gt; CAM_BEV\n    end\n\n    ENC_OUT --&gt; CLONE\n    ENC_OUT --&gt; CONCAT\n\n    subgraph LidarFusion[\"LiDAR Projection &amp; Fusion\"]\n        direction TB\n        RAW[\"Raw LiDAR BEV&lt;br/&gt;(bev_lidar)\"]\n        INTERP[\"F.interpolate&lt;br/&gt;(bilinear, match BEV grid)\"]\n        CONV[\"Conv2d 64 -&gt; 256&lt;br/&gt;(1x1 projection)\"]\n        FLAT[\"Flatten + Permute&lt;br/&gt;(spatial dims to sequence)\"]\n        LN1[\"LayerNorm&lt;br/&gt;(normalize variance)\"]\n        ALPHA[\"Scale by alpha = 5.0&lt;br/&gt;(magnitude matching)\"]\n        CONCAT[\"Concatenate&lt;br/&gt;[camera BEV, LiDAR tokens]&lt;br/&gt;along feature dim\"]\n        LINEAR[\"Linear 2C -&gt; C&lt;br/&gt;(identity-initialized)\"]\n        LN2[\"LayerNorm\"]\n        FUSED[\"Fused BEV&lt;br/&gt;(ready for decoder)\"]\n\n        RAW --&gt; INTERP --&gt; CONV --&gt; FLAT --&gt; LN1 --&gt; ALPHA --&gt; CONCAT\n        CONCAT --&gt; LINEAR --&gt; LN2 --&gt; FUSED\n    end\n\n    FUSED --&gt; DECODER[\"Decoder Cross-Attention&lt;br/&gt;(450 object queries attend&lt;br/&gt;to fused BEV)\"]\n    CAM_BEV --&gt;|\"cross-attn\"| VEL[\"Velocity Head\"]\n\n    classDef cam  fill:#fff3e0,stroke:#ff9800,stroke-width:2px,color:#e65100\n    classDef lid  fill:#e8f5e9,stroke:#4caf50,stroke-width:2px,color:#1b5e20\n    classDef fuse fill:#fce4ec,stroke:#e91e63,stroke-width:2px,color:#880e4f\n    classDef dec  fill:#e0f7fa,stroke:#00acc1,stroke-width:2px,color:#006064\n\n    class ENC_OUT,CLONE,CAM_BEV cam\n    class RAW,INTERP,CONV,FLAT,LN1,ALPHA lid\n    class CONCAT,LINEAR,LN2,FUSED fuse\n    class DECODER,VEL dec</code></pre> <p>The clone step is critical: <code>bev_embed_cam</code> captures the BEV before any decoder-side LiDAR mixing. The velocity head cross-attends to this camera-only copy to preserve the temporal motion signal that would otherwise be diluted by single-frame LiDAR features.</p>"},{"location":"05-decoder-fusion/#3-identity-initialization","title":"3. Identity Initialization","text":"<p>The fusion linear layer (<code>lidar_fuse_linear</code>) maps from 2C dimensions back to C. A naive random initialization would immediately inject noisy LiDAR features into a pipeline that was producing reasonable camera-only BEV representations, destabilizing early training. The solution is identity initialization.</p> <pre><code>graph TD\n    subgraph WeightMatrix[\"Linear Weight Matrix (C x 2C)\"]\n        direction LR\n        LEFT[\"Left Half (columns 0..C-1)&lt;br/&gt;= Identity Matrix I&lt;br/&gt;(camera passthrough)\"]\n        RIGHT[\"Right Half (columns C..2C-1)&lt;br/&gt;= All Zeros&lt;br/&gt;(LiDAR ignored)\"]\n    end\n\n    INPUT[\"Input: [camera_feats, lidar_feats]&lt;br/&gt;concatenated along feature dim\"]\n    OUTPUT[\"Output at init:&lt;br/&gt;camera_feats * I + lidar_feats * 0&lt;br/&gt;= camera_feats unchanged\"]\n\n    INPUT --&gt; WeightMatrix --&gt; OUTPUT\n\n    subgraph Training[\"During Training\"]\n        direction TB\n        GRAD[\"Gradients flow into right half\"]\n        LEARN[\"Network gradually learns&lt;br/&gt;nonzero LiDAR weights\"]\n        RESULT[\"Output evolves to:&lt;br/&gt;camera + learned LiDAR contribution\"]\n        GRAD --&gt; LEARN --&gt; RESULT\n    end\n\n    OUTPUT -.-&gt;|\"training begins\"| GRAD\n\n    classDef cam  fill:#fff3e0,stroke:#ff9800,stroke-width:2px,color:#e65100\n    classDef lid  fill:#e8f5e9,stroke:#4caf50,stroke-width:2px,color:#1b5e20\n    classDef train fill:#f3e5f5,stroke:#9c27b0,stroke-width:2px,color:#4a148c\n\n    class LEFT cam\n    class RIGHT lid\n    class GRAD,LEARN,RESULT train</code></pre> <p>The initialization code makes this explicit:</p> <pre><code># In init_weights():\nself.lidar_fuse_linear.weight.zero_()           # start with all zeros\nself.lidar_fuse_linear.weight[:, :C] = torch.eye(C)  # camera half = identity\n# lidar half (columns C..2C-1) remains zero\n</code></pre> <p>At initialization, the linear layer is a pure passthrough for camera features. The bias is also zeroed. This means the network begins training as if no decoder-side fusion exists at all --- identical to the camera-only baseline. As gradients flow during training, the right half of the weight matrix gradually develops nonzero values, and the network learns exactly how much LiDAR information to incorporate and in what combination.</p> <p>This design pattern is sometimes called a \"residual gate\" or \"zero init shortcut.\" It is especially important here because the encoder output is already a mature, high-quality representation. Disrupting it with random LiDAR noise at the start of training would waste optimization steps recovering lost performance.</p>"},{"location":"05-decoder-fusion/#4-magnitude-matching","title":"4. Magnitude Matching","text":"<p>Even with identity initialization, there is a subtle problem: the raw LiDAR projections and the camera BEV features live at very different scales.</p> <pre><code>graph LR\n    subgraph Problem[\"Scale Mismatch\"]\n        direction TB\n        LIDAR_RAW[\"LiDAR projection output&lt;br/&gt;norm ~ 0.4\"]\n        CAM_BEV[\"Camera BEV features&lt;br/&gt;norm ~ 16\"]\n        MISMATCH[\"40x difference&lt;br/&gt;LiDAR signal vanishes&lt;br/&gt;in concatenation\"]\n        LIDAR_RAW --&gt; MISMATCH\n        CAM_BEV --&gt; MISMATCH\n    end\n\n    subgraph Solution[\"LayerNorm + Alpha Scaling\"]\n        direction TB\n        LN_STEP[\"LayerNorm&lt;br/&gt;norm ~ 1.0\"]\n        ALPHA_STEP[\"Multiply by alpha = 5.0&lt;br/&gt;norm ~ 5.0\"]\n        MATCHED[\"Same order of magnitude&lt;br/&gt;as camera features\"]\n        LN_STEP --&gt; ALPHA_STEP --&gt; MATCHED\n    end\n\n    MISMATCH --&gt;|\"apply to LiDAR tokens\"| LN_STEP\n\n    classDef problem fill:#ffebee,stroke:#c62828,stroke-width:2px,color:#b71c1c\n    classDef solution fill:#e8f5e9,stroke:#2e7d32,stroke-width:2px,color:#1b5e20\n\n    class LIDAR_RAW,CAM_BEV,MISMATCH problem\n    class LN_STEP,ALPHA_STEP,MATCHED solution</code></pre> <p>Why this matters. When two feature vectors are concatenated and fed through a linear layer, the layer's gradients are proportional to the input magnitudes. If LiDAR features have norm ~0.4 while camera features have norm ~16, the LiDAR columns of the weight matrix receive gradients roughly 40x smaller than the camera columns. Learning to use LiDAR information becomes extremely slow.</p> <p>The two-step fix:</p> <ol> <li> <p>LayerNorm normalizes the projected LiDAR tokens to have unit variance (norm ~1.0). This removes the dependence on the raw PointPillars output scale.</p> </li> <li> <p>Alpha scaling (constant factor of 5.0) brings the normalized features up to the same order of magnitude as the camera BEV. The value 5.0 was chosen empirically to produce LiDAR norms (~5) that are comparable to, but slightly below, camera norms (~16), ensuring balanced gradient flow without overwhelming the camera signal.</p> </li> </ol> <pre><code># In forward():\nbev_lidar_tok = F.layer_norm(bev_lidar_tok, (bev_lidar_tok.size(-1),))\nbev_lidar_tok = bev_lidar_tok * 5.0\n</code></pre> <p>Note that this normalization is applied before concatenation, so the LayerNorm here is independent of the <code>lidar_fuse_norm</code> that comes after the linear projection.</p>"},{"location":"05-decoder-fusion/#5-comparison-encoder-side-vs-decoder-side-fusion","title":"5. Comparison: Encoder-Side vs Decoder-Side Fusion","text":"Aspect Encoder-Side Fusion Decoder-Side Fusion Location Inside each encoder layer (applied 4 times) Once, between encoder and decoder Mechanism Dual SCA with learnable blend weight Concatenation + linear projection Initialization Standard Xavier (blend weight starts at sigmoid(-2) ~ 0.12) Identity passthrough (camera = I, LiDAR = 0) Temporal impact Dilutes TSA signal at every layer Single dilution point after encoder completes Granularity Fine-grained, per-layer refinement through deformable attention Coarse, single-shot whole-BEV injection Parameters 1 blend scalar per layer + LiDAR projection conv Linear(2C, C) + LayerNorm + LiDAR projection conv How LiDAR enters Deformable cross-attention queries LiDAR BEV Direct feature concatenation"},{"location":"05-decoder-fusion/#6-why-both","title":"6. Why Both?","text":"<p>The encoder and decoder fusion stages serve fundamentally different roles, and their combination is more effective than either alone.</p> <p>Encoder-side fusion operates through deformable attention, meaning the BEV queries can selectively attend to relevant spatial locations in the LiDAR feature map. This is powerful for fine-grained geometric detail --- depth ambiguities in camera features can be resolved by attending to nearby LiDAR points. However, because the fusion happens inside the encoder's iterative processing (4 layers of attention, normalization, and feed-forward), some LiDAR signal may be attenuated or transformed beyond recognition by the time the encoder output is produced.</p> <p>Decoder-side fusion bypasses the encoder entirely. The LiDAR BEV features are projected and concatenated directly with the finished encoder output. This provides a \"clean\" LiDAR signal to the decoder that has not passed through any encoder layers. The decoder's object queries can therefore draw on both the encoder's refined camera-LiDAR blend and the raw, unprocessed LiDAR geometry.</p> <p>Together, the two stages give the model access to LiDAR information at two levels of abstraction: deeply integrated (encoder) and directly available (decoder). This is analogous to how residual connections in deep networks provide both processed and shortcut signals --- the combination is strictly more expressive than either path alone.</p>"},{"location":"05-decoder-fusion/#7-key-files","title":"7. Key Files","text":"File Lines What to look for <code>projects/mmdet3d_plugin/bevformer/modules/transformer.py</code> 53--62 <code>fusion_mode</code> parameter and validation <code>projects/mmdet3d_plugin/bevformer/modules/transformer.py</code> 94--97 Module definitions: <code>lidar_proj</code>, <code>lidar_fuse_linear</code>, <code>lidar_fuse_norm</code> <code>projects/mmdet3d_plugin/bevformer/modules/transformer.py</code> 160--177 <code>init_weights()</code> --- identity initialization for the fusion linear layer <code>projects/mmdet3d_plugin/bevformer/modules/transformer.py</code> 346 <code>bev_embed_cam = bev_embed.clone()</code> --- camera-only snapshot before fusion <code>projects/mmdet3d_plugin/bevformer/modules/transformer.py</code> 349--364 Forward pass: LiDAR projection, LayerNorm, alpha scaling, concat, linear fusion <p>Previous: Chapter 4 -- Encoder-Side Fusion | Next: Chapter 6 -- Transformer Decoder</p>"},{"location":"06-transformer-decoder/","title":"Chapter 6: Transformer Decoder","text":"<p>00 Overview | 01 Data Pipeline | 02 Camera Branch | 03 LiDAR Branch | 04 Encoder Fusion | 05 Decoder Fusion | 06 Decoder | 07 Detection Heads | 08 Loss &amp; Training | 09 Inference | Appendix A: Tensors | Appendix B: Files</p>"},{"location":"06-transformer-decoder/#overview","title":"Overview","text":"<p>The transformer decoder transforms 450 learned object queries into structured 3D detections. Each query represents a candidate object and progressively refines its understanding by cross-attending to the fused BEV representation. The decoder uses 6 iterative layers with reference point refinement -- each layer's output improves the spatial precision of the next.</p>"},{"location":"06-transformer-decoder/#decoder-architecture","title":"Decoder Architecture","text":"<pre><code>graph TD\n    subgraph Inputs\n        OQ[\"450 Learned Object Queries\"]\n        PE[\"Positional Embeddings\"]\n        BEV[\"Fused BEV from Decoder-Side Fusion\"]\n    end\n\n    OQ --&gt; SPLIT[\"Split query_embed into&lt;br/&gt;query + query_pos\"]\n    PE --&gt; SPLIT\n    SPLIT --&gt; REF[\"Predict Initial Reference Points&lt;br/&gt;Linear + sigmoid \u2192 (x, y, z)\"]\n\n    REF --&gt; L1\n\n    subgraph Layer[\"Decoder Layer (x6)\"]\n        L1[\"Self-Attention&lt;br/&gt;Queries interact with each other\"]\n        L1 --&gt; N1[\"LayerNorm\"]\n        N1 --&gt; L2[\"Cross-Attention to BEV&lt;br/&gt;Deformable attention at&lt;br/&gt;learned sampling offsets\"]\n        L2 --&gt; N2[\"LayerNorm\"]\n        N2 --&gt; L3[\"Feed-Forward Network\"]\n        L3 --&gt; N3[\"LayerNorm\"]\n        N3 --&gt; REFINE[\"Reference Point Refinement&lt;br/&gt;Update (x, y, z) using&lt;br/&gt;regression branch output\"]\n    end\n\n    BEV --&gt; L2\n\n    REFINE --&gt; OUT[\"Collect intermediate outputs&lt;br/&gt;for auxiliary losses\"]\n\n    style Inputs fill:#e8f4fd,stroke:#4a90d9\n    style Layer fill:#fff3e0,stroke:#f5a623</code></pre>"},{"location":"06-transformer-decoder/#how-one-decoder-layer-works","title":"How One Decoder Layer Works","text":""},{"location":"06-transformer-decoder/#1-self-attention","title":"1. Self-Attention","text":"<p>The 450 object queries attend to each other via standard multi-head attention (8 heads, dropout 0.1). This allows queries to reason about relationships between candidate objects -- for example, two nearby queries can coordinate to avoid predicting the same object.</p>"},{"location":"06-transformer-decoder/#2-cross-attention-to-fused-bev","title":"2. Cross-Attention to Fused BEV","text":"<p>Each query attends to the BEV feature map using <code>CustomMSDeformableAttention</code>:</p> <pre><code>graph LR\n    Q[\"Object Query\"] --&gt; OFFSET[\"Predict Sampling&lt;br/&gt;Offsets\"]\n    Q --&gt; WEIGHT[\"Predict Attention&lt;br/&gt;Weights\"]\n    RP[\"Reference Point&lt;br/&gt;(x, y) on BEV\"] --&gt; SAMPLE[\"Sampling Locations =&lt;br/&gt;Reference + Offsets\"]\n    OFFSET --&gt; SAMPLE\n    SAMPLE --&gt; DA[\"Sample BEV Features&lt;br/&gt;at 4 Deformable Points\"]\n    WEIGHT --&gt; DA\n    DA --&gt; OUT[\"Weighted Sum \u2192&lt;br/&gt;Updated Query\"]\n\n    style Q fill:#e8f4fd,stroke:#4a90d9\n    style RP fill:#fff3e0,stroke:#f5a623</code></pre> <p>Key properties: - 1 spatial level: the BEV grid (100 x 100) - 4 deformable sampling points per query per head - 8 attention heads - Reference points are in normalized BEV coordinates (0 to 1)</p>"},{"location":"06-transformer-decoder/#3-feed-forward-network","title":"3. Feed-Forward Network","text":"<p>Standard two-layer MLP: Linear(256, 512) -&gt; ReLU -&gt; Linear(512, 256) with residual connection.</p>"},{"location":"06-transformer-decoder/#4-reference-point-refinement","title":"4. Reference Point Refinement","text":"<p>After each layer, the regression branch predicts coordinate offsets that refine the reference points:</p> <pre><code>graph TD\n    OLD[\"Current Reference Point&lt;br/&gt;(x, y, z) in sigmoid space\"]\n    REG[\"Regression Branch Output&lt;br/&gt;(10-dim bbox code)\"]\n\n    OLD --&gt; INV[\"inverse_sigmoid(x, y, z)\"]\n    REG --&gt; EXTRACT[\"Extract offsets:&lt;br/&gt;dx, dy from indices 0:2&lt;br/&gt;dz from index 4\"]\n\n    INV --&gt; ADD[\"Add: logit + offset\"]\n    EXTRACT --&gt; ADD\n\n    ADD --&gt; SIG[\"sigmoid \u2192 new (x, y, z)\"]\n    SIG --&gt; DET[\"detach&lt;br/&gt;(stop gradient)\"]\n    DET --&gt; NEXT[\"Refined Reference Point&lt;br/&gt;for next layer\"]\n\n    style OLD fill:#e8f4fd,stroke:#4a90d9\n    style NEXT fill:#d4edda,stroke:#28a745</code></pre> <p>The <code>detach()</code> is critical -- gradients do not flow backward through refined references. Each layer learns to predict accurate outputs given its own reference points, rather than learning to produce offsets that are useful for the next layer.</p> <p>Note: The z-offset is read from index 4 of the regression output (the <code>cz</code> slot), not index 2. This matches the bbox code layout: <code>[cx, cy, log_w, log_l, cz, log_h, ...]</code>.</p>"},{"location":"06-transformer-decoder/#iterative-refinement-across-6-layers","title":"Iterative Refinement Across 6 Layers","text":"<pre><code>graph LR\n    subgraph \"Layer 0\"\n        Q0[\"Query\"] --&gt; D0[\"Decode\"]\n        R0[\"Initial Ref\"] --&gt; D0\n        D0 --&gt; Q1[\"Updated Query\"]\n        D0 --&gt; R1[\"Refined Ref\"]\n    end\n\n    subgraph \"Layer 1\"\n        Q1 --&gt; D1[\"Decode\"]\n        R1 --&gt; D1\n        D1 --&gt; Q2[\"Updated Query\"]\n        D1 --&gt; R2[\"Refined Ref\"]\n    end\n\n    subgraph \"...\"\n        Q2 --&gt; D2[\"...\"]\n        R2 --&gt; D2\n    end\n\n    subgraph \"Layer 5\"\n        D2 --&gt; Q5[\"Final Query\"]\n        D2 --&gt; R5[\"Final Ref\"]\n    end\n\n    Q0 -.-&gt; H0[\"Heads \u2192 Predictions\"]\n    Q1 -.-&gt; H1[\"Heads \u2192 Predictions\"]\n    Q5 -.-&gt; H5[\"Heads \u2192 Predictions&lt;br/&gt;(primary output)\"]\n\n    style H5 fill:#d4edda,stroke:#28a745</code></pre> <p>All 6 layers produce predictions (via the detection heads in Chapter 7). The last layer provides the primary output; earlier layers provide auxiliary supervision that stabilizes training.</p>"},{"location":"06-transformer-decoder/#output","title":"Output","text":"<p>The decoder returns two tensors:</p> Output Shape Description <code>inter_states</code> (6, B, 450, 256) Decoder query features at each layer <code>inter_references</code> (6, B, 450, 3) Refined reference points at each layer (sigmoid space) <p>Both are used by the detection heads: <code>inter_states</code> feeds all prediction branches, and <code>inter_references</code> provides the spatial anchor for bbox coordinate decoding.</p>"},{"location":"06-transformer-decoder/#key-files","title":"Key Files","text":"File Path Role <code>decoder.py</code> <code>bevformer/modules/decoder.py</code> <code>DetectionTransformerDecoder</code> and <code>CustomMSDeformableAttention</code> <code>transformer.py</code> <code>bevformer/modules/transformer.py</code> Decoder invocation within <code>PerceptionTransformer.forward</code> <p>Next: Chapter 7 - Detection Heads</p>"},{"location":"07-detection-heads/","title":"Chapter 7: Detection Heads","text":"<p>00 Overview | 01 Data Pipeline | 02 Camera Branch | 03 LiDAR Branch | 04 Encoder Fusion | 05 Decoder Fusion | 06 Decoder | 07 Detection Heads | 08 Loss &amp; Training | 09 Inference | Appendix A: Tensors | Appendix B: Files</p>"},{"location":"07-detection-heads/#overview","title":"Overview","text":"<p>Each decoder layer produces a set of predictions through 5 parallel heads. Three of these are novel additions to BEVFormer: the yaw bin/residual heads and the velocity head with camera-only cross-attention.</p>"},{"location":"07-detection-heads/#head-architecture","title":"Head Architecture","text":"<pre><code>graph TD\n    DEC[\"Decoder Output&lt;br/&gt;hs[layer]\"]\n\n    DEC --&gt; CLS[\"Classification Head&lt;br/&gt;MLP \u2192 10 classes\"]\n    DEC --&gt; REG[\"Regression Head&lt;br/&gt;MLP \u2192 10-dim bbox\"]\n    DEC --&gt; YAW_B[\"Yaw Bin Head&lt;br/&gt;MLP \u2192 24 bins\"]\n    DEC --&gt; YAW_R[\"Yaw Residual Head&lt;br/&gt;MLP \u2192 (sin, cos)\"]\n\n    DEC --&gt; VEL_CA[\"Velocity Cross-Attention\"]\n    CAM_BEV[\"Camera-Only BEV&lt;br/&gt;(no LiDAR fusion)\"] --&gt; VEL_CA\n    VEL_CA --&gt; VEL[\"Velocity Head&lt;br/&gt;MLP \u2192 (vx, vy)\"]\n\n    CLS --&gt; OUT[\"Final Predictions\"]\n    REG --&gt; OUT\n    YAW_B --&gt; OUT\n    YAW_R --&gt; OUT\n    VEL --&gt; OUT\n\n    classDef standard fill:#e8f4fd,stroke:#4a90d9\n    classDef novel fill:#fff3e0,stroke:#f5a623\n    classDef velocity fill:#d4edda,stroke:#28a745\n\n    class CLS,REG standard\n    class YAW_B,YAW_R novel\n    class VEL_CA,VEL,CAM_BEV velocity</code></pre>"},{"location":"07-detection-heads/#head-details","title":"Head Details","text":"Head Architecture Output Notes Classification Linear+LN+ReLU (x N) -&gt; Linear(256, 10) Class scores Focal loss supervision Regression Linear+ReLU (x N) -&gt; Linear(256, 10) 10-dim bbox code Yaw &amp; velocity indices zeroed in loss Yaw Bin Linear(256,256)+ReLU -&gt; Linear(256, 24) 24 bin logits CrossEntropy loss Yaw Residual Linear(256,256)+ReLU -&gt; Linear(256, 2) (sin_res, cos_res) SmoothL1 loss Velocity CrossAttn + Linear(256,256)+ReLU -&gt; Linear(256, 2) (vx, vy) Camera-only BEV input <p>All heads are cloned per decoder layer (6 independent copies each), enabling auxiliary supervision.</p>"},{"location":"07-detection-heads/#bbox-code-layout","title":"BBox Code Layout","text":"<p>The regression head outputs a 10-dimensional vector with this fixed layout:</p> Index Symbol Encoding Supervised By 0 cx sigmoid + pc_range scaling <code>reg_branches</code> 1 cy sigmoid + pc_range scaling <code>reg_branches</code> 2 log(w) log-space width <code>reg_branches</code> 3 log(l) log-space length <code>reg_branches</code> 4 cz sigmoid + pc_range scaling <code>reg_branches</code> 5 log(h) log-space height <code>reg_branches</code> 6 sin(yaw) unit-circle normalized <code>yaw_bin/res_branches</code> only 7 cos(yaw) unit-circle normalized <code>yaw_bin/res_branches</code> only 8 vx direct <code>vel_branches</code> only 9 vy direct <code>vel_branches</code> only <p>Indices 6-9 are overwritten at inference by the dedicated yaw and velocity heads. During training, their loss weights in <code>bbox_weights</code> are set to zero to prevent conflicting gradients.</p>"},{"location":"07-detection-heads/#yaw-binresidual-head","title":"Yaw Bin/Residual Head","text":""},{"location":"07-detection-heads/#the-problem-with-direct-yaw-regression","title":"The Problem with Direct Yaw Regression","text":"<p>Yaw angles have a wrapping discontinuity at +/-pi. A small angular change near the boundary causes a huge regression target jump, destabilizing gradients.</p>"},{"location":"07-detection-heads/#the-bin-residual-solution","title":"The Bin + Residual Solution","text":"<pre><code>graph TD\n    subgraph \"Encoding (Training)\"\n        YAW[\"Ground Truth Yaw\"] --&gt; BIN_IDX[\"Bin Index&lt;br/&gt;floor((yaw + pi) / bin_size)&lt;br/&gt;24 bins, 15 deg each\"]\n        YAW --&gt; CENTER[\"Bin Center&lt;br/&gt;(bin_idx + 0.5) * bin_size - pi\"]\n        YAW --&gt; RES[\"Residual = yaw - center&lt;br/&gt;Small angle within +/-7.5 deg\"]\n        RES --&gt; SINCOS[\"Encode as (sin, cos)&lt;br/&gt;Avoids wrapping issues\"]\n    end\n\n    subgraph \"Decoding (Inference)\"\n        PRED_BIN[\"Predicted Bin Logits\"] --&gt; ARGMAX[\"argmax \u2192 winning bin\"]\n        ARGMAX --&gt; PRED_CENTER[\"Bin Center\"]\n        PRED_RES[\"Predicted (sin, cos)\"] --&gt; ATAN[\"atan2(sin, cos) \u2192 residual angle\"]\n        PRED_CENTER --&gt; FINAL[\"Final Yaw = center + residual\"]\n        ATAN --&gt; FINAL\n    end\n\n    style SINCOS fill:#d4edda,stroke:#28a745\n    style FINAL fill:#d4edda,stroke:#28a745</code></pre> <p>Why this works: The bin classification handles coarse orientation (no wrapping issues with cross-entropy), while the residual regression handles fine-grained correction within a narrow +/-7.5 degree range using sin/cos encoding.</p>"},{"location":"07-detection-heads/#sincos-normalization-in-regression-head","title":"Sin/Cos Normalization in Regression Head","text":"<p>The regression branch also outputs sin/cos at indices 6-7. These are stabilized during training:</p> <pre><code>sc = reg_output[..., 6:8].tanh()                        # clamp to (-1, 1)\nsc = sc / (sc.norm(dim=-1, keepdim=True) + 1e-7)        # project onto unit circle\n</code></pre> <p>This prevents the sin/cos pair from drifting off the unit circle during early training.</p>"},{"location":"07-detection-heads/#velocity-head","title":"Velocity Head","text":"<p>The velocity head is the key innovation for maintaining accurate motion prediction despite LiDAR fusion.</p>"},{"location":"07-detection-heads/#the-temporal-dilution-problem","title":"The Temporal Dilution Problem","text":"<pre><code>graph LR\n    subgraph \"Temporal Self-Attention\"\n        PREV[\"Previous BEV&lt;br/&gt;(frame t-1)\"] --&gt; TSA[\"TSA compares&lt;br/&gt;current vs previous\"]\n        CUR[\"Current BEV&lt;br/&gt;(frame t)\"] --&gt; TSA\n        TSA --&gt; MOTION[\"Motion Signal&lt;br/&gt;encoded in BEV\"]\n    end\n\n    subgraph \"The Problem\"\n        MOTION --&gt; BLEND[\"Blended with&lt;br/&gt;static LiDAR features\"]\n        LIDAR[\"LiDAR BEV&lt;br/&gt;(single frame,&lt;br/&gt;no temporal info)\"] --&gt; BLEND\n        BLEND --&gt; DILUTED[\"Diluted&lt;br/&gt;Motion Signal\"]\n    end\n\n    style MOTION fill:#d4edda,stroke:#28a745\n    style DILUTED fill:#f8d7da,stroke:#dc3545\n    style LIDAR fill:#fff3e0,stroke:#f5a623</code></pre>"},{"location":"07-detection-heads/#the-solution-camera-only-cross-attention","title":"The Solution: Camera-Only Cross-Attention","text":"<pre><code>graph TD\n    subgraph \"Velocity Head\"\n        DQ[\"Decoder Query&lt;br/&gt;(object representation)\"]\n        CAM[\"Camera-Only BEV&lt;br/&gt;(cloned BEFORE&lt;br/&gt;LiDAR fusion)\"]\n\n        DQ --&gt;|\"Q\"| MHA[\"Multi-Head Attention&lt;br/&gt;8 heads\"]\n        CAM --&gt;|\"K, V\"| MHA\n\n        MHA --&gt; CTX[\"Velocity Context&lt;br/&gt;(motion-aware features)\"]\n        CTX --&gt; MLP[\"MLP: 256 \u2192 256 \u2192 2\"]\n        MLP --&gt; VEL[\"Velocity (vx, vy)\"]\n    end\n\n    subgraph \"Why Camera-Only BEV?\"\n        P1[\"Preserves full temporal signal from TSA\"]\n        P2[\"No dilution from static LiDAR geometry\"]\n        P3[\"Each query selectively attends to&lt;br/&gt;motion-relevant BEV regions\"]\n    end\n\n    style CAM fill:#d4edda,stroke:#28a745\n    style VEL fill:#d4edda,stroke:#28a745</code></pre> <p>Key design choices:</p> <ol> <li>Camera-only BEV: <code>bev_embed_cam</code> is cloned before decoder-side LiDAR fusion, preserving the full temporal motion signal from TSA</li> <li>Full attention (not deformable): <code>nn.MultiheadAttention</code> allows each query to attend over the entire BEV grid, important for capturing motion patterns that may span large regions</li> <li>Per-layer: One cross-attention + MLP per decoder layer, receiving the progressively refined query</li> </ol>"},{"location":"07-detection-heads/#gradient-isolation","title":"Gradient Isolation","text":"<p>A critical design pattern ensures each specialized head receives exclusive supervision:</p> <pre><code>graph TD\n    subgraph \"Regression Head Output (10-dim)\"\n        R0[\"cx\"] --- R1[\"cy\"]\n        R1 --- R2[\"log_w\"]\n        R2 --- R3[\"log_l\"]\n        R3 --- R4[\"cz\"]\n        R4 --- R5[\"log_h\"]\n        R5 --- R6[\"sin_yaw\"]\n        R6 --- R7[\"cos_yaw\"]\n        R7 --- R8[\"vx\"]\n        R8 --- R9[\"vy\"]\n    end\n\n    R0 --&gt; L1[\"loss_bbox&lt;br/&gt;(L1 loss)\"]\n    R1 --&gt; L1\n    R2 --&gt; L1\n    R3 --&gt; L1\n    R4 --&gt; L1\n    R5 --&gt; L1\n\n    R6 -.-&gt;|\"weight = 0\"| L1\n    R7 -.-&gt;|\"weight = 0\"| L1\n    R8 -.-&gt;|\"weight = 0\"| L1\n    R9 -.-&gt;|\"weight = 0\"| L1\n\n    YB[\"Yaw Bin Head\"] --&gt; L2[\"loss_yaw_bin&lt;br/&gt;(CrossEntropy)\"]\n    YR[\"Yaw Res Head\"] --&gt; L3[\"loss_yaw_res&lt;br/&gt;(SmoothL1)\"]\n    VH[\"Velocity Head\"] --&gt; L4[\"loss_vel&lt;br/&gt;(SmoothL1)\"]\n\n    classDef active fill:#d4edda,stroke:#28a745\n    classDef zeroed fill:#f8d7da,stroke:#dc3545\n\n    class R0,R1,R2,R3,R4,R5 active\n    class R6,R7,R8,R9 zeroed</code></pre> <p>By zeroing <code>bbox_weights</code> at indices 6-9, the regression head receives no gradient for yaw or velocity. All yaw learning flows through the bin + residual heads, and all velocity learning flows through the camera-only cross-attention head.</p>"},{"location":"07-detection-heads/#key-files","title":"Key Files","text":"File Path Role <code>bevformer_head.py</code> <code>bevformer/dense_heads/bevformer_head.py</code> All prediction heads, velocity cross-attention, loss computation <code>util.py</code> <code>core/bbox/util.py</code> <code>normalize_bbox</code>, <code>denormalize_bbox</code>, yaw bin encode/decode <p>Next: Chapter 8 - Loss Functions &amp; Training</p>"},{"location":"08-loss-and-training/","title":"Chapter 8: Loss Functions &amp; Training","text":"<p>00 Overview | 01 Data Pipeline | 02 Camera Branch | 03 LiDAR Branch | 04 Encoder Fusion | 05 Decoder Fusion | 06 Decoder | 07 Detection Heads | 08 Loss &amp; Training | 09 Inference | Appendix A: Tensors | Appendix B: Files</p>"},{"location":"08-loss-and-training/#overview","title":"Overview","text":"<p>Training combines five loss functions with Hungarian matching, auxiliary decoder losses, and careful gradient isolation. The system runs in full FP32 precision for maximum numerical stability.</p>"},{"location":"08-loss-and-training/#loss-components","title":"Loss Components","text":"Loss Type Weight Scope Description <code>loss_cls</code> Focal Loss 2.0 All 450 queries 10-class classification with background suppression <code>loss_bbox</code> L1 Loss 0.25 Positive queries 10-dim bbox regression (yaw &amp; velocity zeroed) <code>loss_yaw_bin</code> Cross Entropy 0.2 Positive queries 24-bin yaw classification <code>loss_yaw_res</code> Smooth L1 0.2 Positive queries (sin, cos) residual regression <code>loss_vel</code> Smooth L1 0.25 Positive queries (vx, vy) from camera-only velocity head"},{"location":"08-loss-and-training/#loss-computation-flow","title":"Loss Computation Flow","text":"<pre><code>graph TD\n    subgraph \"Per Decoder Layer\"\n        GT[\"Ground Truth Boxes&lt;br/&gt;+ Labels\"] --&gt; MATCH[\"Hungarian Matching&lt;br/&gt;(one-to-one assignment)\"]\n        PRED[\"Predicted Boxes&lt;br/&gt;+ Scores\"] --&gt; MATCH\n\n        MATCH --&gt; POS[\"Positive Queries&lt;br/&gt;(matched to GT)\"]\n        MATCH --&gt; NEG[\"Negative Queries&lt;br/&gt;(background)\"]\n\n        POS --&gt; LCLS[\"loss_cls&lt;br/&gt;Focal Loss on all queries\"]\n        NEG --&gt; LCLS\n\n        POS --&gt; LBBOX[\"loss_bbox&lt;br/&gt;L1 on dims 0-5 only&lt;br/&gt;(yaw &amp; vel weights = 0)\"]\n\n        POS --&gt; LYAW_B[\"loss_yaw_bin&lt;br/&gt;CrossEntropy on 24 bins\"]\n        POS --&gt; LYAW_R[\"loss_yaw_res&lt;br/&gt;SmoothL1 on (sin, cos)\"]\n        POS --&gt; LVEL[\"loss_vel&lt;br/&gt;SmoothL1 on (vx, vy)\"]\n    end\n\n    subgraph \"Multi-Layer Aggregation\"\n        L0[\"Layer 0 losses\"] --&gt; AUX[\"Auxiliary losses&lt;br/&gt;(d0.loss_cls, d0.loss_bbox, ...)\"]\n        L1[\"Layer 1 losses\"] --&gt; AUX\n        L4[\"Layer 4 losses\"] --&gt; AUX\n        L5[\"Layer 5 losses\"] --&gt; PRIMARY[\"Primary losses&lt;br/&gt;(loss_cls, loss_bbox, ...)\"]\n    end\n\n    LCLS --&gt; L5\n    LBBOX --&gt; L5\n    LYAW_B --&gt; L5\n    LYAW_R --&gt; L5\n    LVEL --&gt; L5\n\n    style PRIMARY fill:#d4edda,stroke:#28a745\n    style AUX fill:#fff3e0,stroke:#f5a623</code></pre>"},{"location":"08-loss-and-training/#gradient-isolation-detail","title":"Gradient Isolation Detail","text":"<p>The bbox regression loss operates on all 10 dimensions, but indices 6-9 have their loss weights zeroed:</p> <pre><code>bbox_weights[:, 6] = 0.0   # sin(yaw) -- yaw_bin/res heads only\nbbox_weights[:, 7] = 0.0   # cos(yaw) -- yaw_bin/res heads only\nbbox_weights[:, 8] = 0.0   # vx -- velocity head only\nbbox_weights[:, 9] = 0.0   # vy -- velocity head only\n</code></pre> <p>This ensures that yaw is exclusively supervised through the bin + residual heads, and velocity is exclusively supervised through the camera-only cross-attention head.</p>"},{"location":"08-loss-and-training/#hungarian-matching","title":"Hungarian Matching","text":"<p>Assignment uses <code>HungarianAssigner3D</code> to find the optimal one-to-one matching between predictions and ground truth:</p> Matching Cost Type Weight Classification Focal Loss Cost 2.0 Bbox regression BBox3D L1 Cost 0.25 IoU IoU Cost 0.25 <p>The matching is performed independently per decoder layer (since predictions differ across layers).</p>"},{"location":"08-loss-and-training/#auxiliary-losses","title":"Auxiliary Losses","text":"<p>All 6 decoder layers produce predictions, but they serve different roles:</p> Layers 0-4 Layer 5 Auxiliary supervision Primary supervision Prefixed: <code>d0.loss_cls</code>, <code>d1.loss_bbox</code>, ... Keys: <code>loss_cls</code>, <code>loss_bbox</code>, ... Same weights as primary Standard weights Stabilize training of earlier layers Drives final detection quality"},{"location":"08-loss-and-training/#training-configuration","title":"Training Configuration","text":""},{"location":"08-loss-and-training/#optimizer","title":"Optimizer","text":"Parameter Value Type AdamW Learning rate 2e-4 Weight decay 0.01 Backbone lr multiplier 0.1 (slower learning for pretrained ResNet50) Gradient clipping max_norm = 1.0"},{"location":"08-loss-and-training/#learning-rate-schedule","title":"Learning Rate Schedule","text":"<pre><code>graph LR\n    subgraph \"LR Schedule\"\n        W[\"Warmup&lt;br/&gt;0 \u2192 10K iters&lt;br/&gt;Linear, ratio 1/1000\"] --&gt; C[\"Cosine Annealing&lt;br/&gt;10K \u2192 200K iters&lt;br/&gt;min_lr_ratio = 1e-3\"]\n    end\n\n    style W fill:#fff3e0,stroke:#f5a623\n    style C fill:#e8f4fd,stroke:#4a90d9</code></pre>"},{"location":"08-loss-and-training/#general-settings","title":"General Settings","text":"Parameter Value Total iterations 200,000 Precision FP32 (full) Batch size 1 per GPU Workers per GPU 6 Eval interval every 20K iterations Checkpoint interval every 20K iterations (keep 5) Speed ~0.39 s/iter GPU memory ~8.9 GB"},{"location":"08-loss-and-training/#temporal-bev-memory-during-training","title":"Temporal BEV Memory During Training","text":"<p>Training uses a temporal queue of 4 frames to build the <code>prev_bev</code> context:</p> <pre><code>graph TD\n    subgraph \"Temporal Queue Processing\"\n        IMG[\"Stacked Images&lt;br/&gt;(B, T=4, 6, C, H, W)\"]\n        IMG --&gt; SPLIT[\"Split into&lt;br/&gt;history (T-1 frames)&lt;br/&gt;+ current (1 frame)\"]\n\n        SPLIT --&gt; HIST[\"obtain_history_bev\"]\n        SPLIT --&gt; CUR[\"Current Frame\"]\n    end\n\n    subgraph \"History BEV Construction\"\n        HIST --&gt; LOOP[\"For each history frame:&lt;br/&gt;1. Extract image features&lt;br/&gt;2. Run encoder only (no decoder)&lt;br/&gt;3. Get BEV embedding\"]\n        LOOP --&gt; |\"eval mode&lt;br/&gt;no gradient&lt;br/&gt;FP32\"| PREV[\"prev_bev&lt;br/&gt;(last history frame's BEV)\"]\n    end\n\n    subgraph \"Current Frame Training\"\n        CUR --&gt; EXTRACT[\"Extract features&lt;br/&gt;(camera + LiDAR)\"]\n        PREV --&gt; FWD[\"forward_pts_train&lt;br/&gt;Full encoder + decoder&lt;br/&gt;with prev_bev context\"]\n        EXTRACT --&gt; FWD\n        FWD --&gt; LOSS[\"Compute all losses\"]\n        FWD --&gt; CACHE[\"Push bev_embed&lt;br/&gt;to scene cache&lt;br/&gt;for next sample\"]\n    end\n\n    style PREV fill:#d4edda,stroke:#28a745\n    style LOSS fill:#e8f4fd,stroke:#4a90d9</code></pre> <p>Key details: - History frames are processed in eval mode with no gradient to save memory - If the scene changes between frames, <code>prev_bev</code> is set to <code>None</code> - The BEV embedding is cached per-scene for cross-sample temporal continuity</p>"},{"location":"08-loss-and-training/#key-files","title":"Key Files","text":"File Path Role <code>bevformer_head.py</code> <code>bevformer/dense_heads/bevformer_head.py</code> <code>loss_single()</code> and <code>loss()</code> methods <code>bevformer.py</code> <code>bevformer/detectors/bevformer.py</code> <code>forward_train()</code>, <code>obtain_history_bev()</code> <code>bevformer_project.py</code> <code>configs/bevformer/bevformer_project.py</code> All training hyperparameters <code>hungarian_assigner_3d.py</code> <code>core/bbox/assigners/hungarian_assigner_3d.py</code> Hungarian matching <p>Next: Chapter 9 - Inference &amp; Decoding</p>"},{"location":"09-inference/","title":"Chapter 9: Inference &amp; Decoding","text":"<p>00 Overview | 01 Data Pipeline | 02 Camera Branch | 03 LiDAR Branch | 04 Encoder Fusion | 05 Decoder Fusion | 06 Decoder | 07 Detection Heads | 08 Loss &amp; Training | 09 Inference | Appendix A: Tensors | Appendix B: Files</p>"},{"location":"09-inference/#overview","title":"Overview","text":"<p>Inference uses NMS-free decoding: the top-K scoring queries are selected, their yaw and velocity slots are overwritten by the dedicated heads, bounding boxes are denormalized, and spatial filtering is applied. No Non-Maximum Suppression is needed because the DETR-style Hungarian matching already ensures one-to-one query-to-object assignment during training.</p>"},{"location":"09-inference/#decoding-pipeline","title":"Decoding Pipeline","text":"<pre><code>graph TD\n    subgraph \"Score Selection\"\n        CLS[\"Class Scores&lt;br/&gt;(450 queries x 10 classes)\"]\n        CLS --&gt; SIG[\"Sigmoid Activation\"]\n        SIG --&gt; FLAT[\"Flatten to 4500 scores\"]\n        FLAT --&gt; TOPK[\"Top-K Selection&lt;br/&gt;(K = 300)\"]\n        TOPK --&gt; IDX[\"Extract:&lt;br/&gt;label = index % 10&lt;br/&gt;query_id = index // 10\"]\n    end\n\n    subgraph \"Head Overrides\"\n        IDX --&gt; GATHER[\"Gather matched&lt;br/&gt;bbox, yaw, velocity predictions\"]\n        GATHER --&gt; YAW_OVR[\"Yaw Override&lt;br/&gt;Bin argmax + residual atan2&lt;br/&gt;\u2192 overwrite sin/cos slots\"]\n        YAW_OVR --&gt; VEL_OVR[\"Velocity Override&lt;br/&gt;vel_preds \u2192 overwrite vx/vy slots\"]\n    end\n\n    subgraph \"Denormalization\"\n        VEL_OVR --&gt; DENORM[\"Denormalize BBox:&lt;br/&gt;exp() for w, l, h&lt;br/&gt;atan2 for yaw&lt;br/&gt;cx, cy, cz, vx, vy direct\"]\n    end\n\n    subgraph \"Post-Processing\"\n        DENORM --&gt; RANGE[\"Spatial Range Filter&lt;br/&gt;Keep boxes within&lt;br/&gt;[-61.2, -61.2, -10] to [61.2, 61.2, 10]\"]\n        RANGE --&gt; SCORE[\"Score Threshold&lt;br/&gt;(default 0.25)\"]\n        SCORE --&gt; SIZE[\"Optional Size Clamp&lt;br/&gt;[0.2, 8.0]\"]\n        SIZE --&gt; OUT[\"Final Detections&lt;br/&gt;(bboxes, scores, labels)\"]\n    end\n\n    style TOPK fill:#e8f4fd,stroke:#4a90d9\n    style YAW_OVR fill:#fff3e0,stroke:#f5a623\n    style VEL_OVR fill:#d4edda,stroke:#28a745\n    style OUT fill:#d4edda,stroke:#28a745</code></pre>"},{"location":"09-inference/#yaw-decoding-detail","title":"Yaw Decoding Detail","text":"<p>The yaw angle is reconstructed from the bin classification and residual regression:</p> <pre><code>graph LR\n    BIN[\"Bin Logits&lt;br/&gt;(24 values)\"] --&gt; ARGMAX[\"argmax&lt;br/&gt;\u2192 winning bin index\"]\n    ARGMAX --&gt; CENTER[\"Bin Center =&lt;br/&gt;-pi + (idx + 0.5) * 15deg\"]\n\n    RES[\"Residual (sin, cos)\"] --&gt; NORM[\"Normalize to&lt;br/&gt;unit circle\"]\n    NORM --&gt; ATAN[\"atan2(sin, cos)&lt;br/&gt;\u2192 residual angle\"]\n\n    CENTER --&gt; ADD[\"Final Yaw =&lt;br/&gt;center + residual\"]\n    ATAN --&gt; ADD\n\n    ADD --&gt; WRITE[\"Write to bbox:&lt;br/&gt;slot 6 = sin(yaw)&lt;br/&gt;slot 7 = cos(yaw)\"]\n\n    style ADD fill:#d4edda,stroke:#28a745</code></pre>"},{"location":"09-inference/#velocity-override","title":"Velocity Override","text":"<p>Simple replacement: the velocity head's output directly overwrites the regression branch's velocity slots:</p> <pre><code>bbox_preds[..., 8:10] = vel_preds    # (vx, vy) from camera-only BEV head\n</code></pre>"},{"location":"09-inference/#test-time-temporal-processing","title":"Test-Time Temporal Processing","text":"<p>During inference, the system maintains temporal context across frames within a scene:</p> <pre><code>graph TD\n    subgraph \"Frame-by-Frame Inference\"\n        NEW_FRAME[\"New Frame Arrives\"]\n        NEW_FRAME --&gt; CHECK[\"Same scene&lt;br/&gt;as previous?\"]\n\n        CHECK --&gt;|\"No (new scene)\"| RESET[\"Reset prev_bev = None&lt;br/&gt;Reset prev_pos, prev_angle\"]\n        CHECK --&gt;|\"Yes (same scene)\"| EGO[\"Compute Ego Motion&lt;br/&gt;delta_pos = cur_pos - prev_pos&lt;br/&gt;delta_angle = cur_angle - prev_angle\"]\n\n        RESET --&gt; FWD[\"Forward Pass\"]\n        EGO --&gt; ROTATE[\"Rotate prev_bev&lt;br/&gt;by ego yaw delta\"]\n        ROTATE --&gt; FWD\n\n        FWD --&gt; DET[\"Detections\"]\n        FWD --&gt; SAVE[\"Save bev_embed&lt;br/&gt;as next prev_bev\"]\n        FWD --&gt; UPDATE[\"Update prev_pos,&lt;br/&gt;prev_angle\"]\n    end\n\n    style DET fill:#d4edda,stroke:#28a745</code></pre> <p>Key details: - Scene token tracking detects scene boundaries (new sequence = reset temporal state) - <code>prev_bev</code> is rotated to compensate for ego-vehicle yaw change using <code>torchvision.transforms.functional.rotate</code> - CAN bus provides the relative ego-motion delta (translation and yaw) - When <code>prev_bev</code> is None (first frame or new scene), TSA is skipped and the system operates in single-frame mode</p>"},{"location":"09-inference/#inference-configuration","title":"Inference Configuration","text":"Parameter Value Score threshold 0.25 Max detections 300 Post-center range [-61.2, -61.2, -10.0, 61.2, 61.2, 10.0] NMS None (NMS-free) Video test mode True (carry prev_bev across frames)"},{"location":"09-inference/#why-nms-free","title":"Why NMS-Free?","text":"<p>The DETR-style decoder with Hungarian matching trains each query to specialize in detecting one object. Unlike anchor-based detectors that produce many overlapping proposals for each object, the one-to-one matching ensures queries learn non-overlapping responsibilities. At inference, simple top-K selection is sufficient.</p>"},{"location":"09-inference/#key-files","title":"Key Files","text":"File Path Role <code>nms_free_coder.py</code> <code>core/bbox/coders/nms_free_coder.py</code> <code>decode_single()</code>: top-K, yaw/vel override, denormalize <code>util.py</code> <code>core/bbox/util.py</code> <code>denormalize_bbox()</code>, <code>overwrite_sincos_from_bins()</code> <code>bevformer.py</code> <code>bevformer/detectors/bevformer.py</code> <code>forward_test()</code>, <code>simple_test()</code>: temporal state management <p>Next: Appendix A - Tensor Shape Reference</p>"},{"location":"appendix-file-map/","title":"Appendix B: File Map &amp; Class Hierarchy","text":"<p>00 Overview | 01 Data Pipeline | 02 Camera Branch | 03 LiDAR Branch | 04 Encoder Fusion | 05 Decoder Fusion | 06 Decoder | 07 Detection Heads | 08 Loss &amp; Training | 09 Inference | Appendix A: Tensors | Appendix B: Files</p>"},{"location":"appendix-file-map/#key-files","title":"Key Files","text":"<p>All paths are relative to <code>projects/mmdet3d_plugin/</code> unless otherwise noted.</p> File Role Chapter <code>bevformer/detectors/bevformer.py</code> Top-level detector: LiDAR branch (PointPillars), temporal BEV memory, train/test orchestration 03, 08 <code>bevformer/modules/transformer.py</code> <code>PerceptionTransformer</code>: BEV construction, encoder/decoder-side fusion, <code>bev_embed_cam</code> creation 04, 05 <code>bevformer/modules/encoder.py</code> <code>BEVFormerEncoder</code> + <code>MM_BEVFormerLayer</code>: encoder loop, dual SCA, learnable blend weights 04 <code>bevformer/modules/spatial_cross_attention.py</code> <code>SpatialCrossAttention</code> + <code>MSDeformableAttention3D</code>: camera cross-attention with sparse re-batching 04 <code>bevformer/modules/temporal_self_attention.py</code> <code>TemporalSelfAttention</code>: prev/cur BEV temporal fusion via deformable attention 04 <code>bevformer/modules/decoder.py</code> <code>DetectionTransformerDecoder</code> + <code>CustomMSDeformableAttention</code>: iterative decoder, reference refinement 06 <code>bevformer/dense_heads/bevformer_head.py</code> <code>BEVFormerHead</code>: all prediction heads, velocity cross-attn, yaw bin/res, loss computation 07, 08 <code>core/bbox/coders/nms_free_coder.py</code> NMS-free bbox coder: top-K decoding, yaw/velocity override, post-center filtering 09 <code>core/bbox/util.py</code> <code>normalize_bbox</code>, <code>denormalize_bbox</code>, yaw bin encode/decode 07, 09 <code>core/bbox/assigners/hungarian_assigner_3d.py</code> Hungarian matching for DETR-style training 08 <code>datasets/nuscenes_dataset.py</code> <code>CustomNuScenesDataset</code>: temporal queue, CAN bus, camera matrices 01 <code>datasets/pipelines/loading.py</code> Point cloud and image loading transforms 01 <code>datasets/pipelines/transform_3d.py</code> Augmentation, normalization, formatting transforms 01 <code>bevformer/modules/custom_base_transformer_layer.py</code> Base transformer layer that builds attentions, norms, FFNs from config 04 <code>../configs/bevformer/bevformer_project.py</code> Full training configuration (all hyperparameters) 08"},{"location":"appendix-file-map/#class-hierarchy","title":"Class Hierarchy","text":"<pre><code>classDiagram\n    class BEVFormer {\n        +extract_pts_bev_feat()\n        +forward_train()\n        +forward_test()\n        +obtain_history_bev()\n    }\n    class PerceptionTransformer {\n        +get_bev_features()\n        +forward()\n        -lidar_encoder_proj\n        -lidar_proj\n        -lidar_fuse_linear\n    }\n    class BEVFormerEncoder {\n        +forward()\n        +get_reference_points()\n        +point_sampling()\n    }\n    class MM_BEVFormerLayer {\n        +forward()\n        -cross_model_weights_logit\n        -lidar_cross_attn_layer\n    }\n    class SpatialCrossAttention {\n        +forward()\n        -MSDeformableAttention3D\n    }\n    class TemporalSelfAttention {\n        +forward()\n        -sampling_offsets\n        -attention_weights\n    }\n    class DetectionTransformerDecoder {\n        +forward()\n    }\n    class CustomMSDeformableAttention {\n        +forward()\n        -sampling_offsets\n        -attention_weights\n    }\n    class BEVFormerHead {\n        +forward()\n        +loss()\n        -cls_branches\n        -reg_branches\n        -yaw_bin_branches\n        -yaw_res_branches\n        -vel_branches\n        -vel_cross_attn\n    }\n    class NMSFreeCoder {\n        +decode()\n        +decode_single()\n    }\n    class CustomNuScenesDataset {\n        +prepare_train_data()\n        +union2one()\n        +get_data_info()\n    }\n\n    BEVFormer --&gt; PerceptionTransformer : pts_bbox_head.transformer\n    BEVFormer --&gt; BEVFormerHead : pts_bbox_head\n    PerceptionTransformer --&gt; BEVFormerEncoder : encoder\n    PerceptionTransformer --&gt; DetectionTransformerDecoder : decoder\n    BEVFormerEncoder --&gt; MM_BEVFormerLayer : layers (x4)\n    MM_BEVFormerLayer --&gt; SpatialCrossAttention : attentions[1]\n    MM_BEVFormerLayer --&gt; TemporalSelfAttention : attentions[0]\n    MM_BEVFormerLayer --&gt; CustomMSDeformableAttention : lidar_cross_attn_layer\n    DetectionTransformerDecoder --&gt; CustomMSDeformableAttention : layer cross-attn\n    BEVFormerHead --&gt; NMSFreeCoder : bbox_coder</code></pre>"},{"location":"appendix-file-map/#class-descriptions","title":"Class Descriptions","text":""},{"location":"appendix-file-map/#detector-layer","title":"Detector Layer","text":"Class Parent Description <code>BEVFormer</code> <code>MVXTwoStageDetector</code> Top-level detector. Manages camera backbone, LiDAR PointPillars branch, temporal BEV cache, and orchestrates train/test flow."},{"location":"appendix-file-map/#transformer-layer","title":"Transformer Layer","text":"Class Parent Description <code>PerceptionTransformer</code> <code>BaseModule</code> Central transformer. Builds BEV features from camera inputs, applies encoder/decoder-side LiDAR fusion, runs decoder. <code>BEVFormerEncoder</code> <code>TransformerLayerSequence</code> 4-layer encoder. Manages reference point generation, point sampling, and the encoder loop. <code>DetectionTransformerDecoder</code> <code>TransformerLayerSequence</code> 6-layer decoder with iterative reference point refinement."},{"location":"appendix-file-map/#encoder-components","title":"Encoder Components","text":"Class Parent Description <code>MM_BEVFormerLayer</code> <code>MyCustomBaseTransformerLayer</code> Multi-modal encoder layer. Runs TSA, dual SCA (camera + LiDAR), learnable blend, FFN. <code>BEVFormerLayer</code> <code>MyCustomBaseTransformerLayer</code> Camera-only baseline encoder layer (not used in fusion mode). <code>SpatialCrossAttention</code> <code>BaseModule</code> Camera cross-attention with sparse re-batching per camera. <code>MSDeformableAttention3D</code> <code>BaseModule</code> Inner deformable attention kernel with pillar-height Z-anchor expansion. <code>TemporalSelfAttention</code> <code>BaseModule</code> Fuses current and previous BEV via deformable attention with temporal stacking. <code>CustomMSDeformableAttention</code> <code>BaseModule</code> Standard deformable attention. Used for both LiDAR SCA (encoder) and BEV cross-attn (decoder)."},{"location":"appendix-file-map/#detection-head","title":"Detection Head","text":"Class Parent Description <code>BEVFormerHead</code> <code>DETRHead</code> All prediction heads (cls, bbox, yaw bin/res, velocity), Hungarian matching, loss computation. <code>NMSFreeCoder</code> <code>BaseBBoxCoder</code> Inference decoding: top-K selection, yaw/velocity override, denormalization, spatial filtering."},{"location":"appendix-file-map/#dataset","title":"Dataset","text":"Class Parent Description <code>CustomNuScenesDataset</code> <code>NuScenesDataset</code> Temporal queue construction, CAN bus computation, camera matrix building."},{"location":"appendix-file-map/#module-ownership-summary","title":"Module Ownership Summary","text":"<pre><code>graph TD\n    subgraph \"Encoder-Side Fusion\"\n        A[\"lidar_encoder_proj&lt;br/&gt;(transformer.py)\"]\n        B[\"cross_model_weights_logit&lt;br/&gt;(encoder.py, per layer)\"]\n        C[\"lidar_cross_attn_layer&lt;br/&gt;(encoder.py, per layer)\"]\n    end\n\n    subgraph \"Decoder-Side Fusion\"\n        D[\"lidar_proj&lt;br/&gt;(transformer.py)\"]\n        E[\"lidar_fuse_linear&lt;br/&gt;(transformer.py)\"]\n        F[\"lidar_fuse_norm&lt;br/&gt;(transformer.py)\"]\n    end\n\n    subgraph \"Velocity Head\"\n        G[\"vel_cross_attn&lt;br/&gt;(bevformer_head.py, per layer)\"]\n        H[\"vel_branches&lt;br/&gt;(bevformer_head.py, per layer)\"]\n    end\n\n    subgraph \"Yaw Heads\"\n        I[\"yaw_bin_branches&lt;br/&gt;(bevformer_head.py, per layer)\"]\n        J[\"yaw_res_branches&lt;br/&gt;(bevformer_head.py, per layer)\"]\n    end\n\n    classDef encoder fill:#e8f4fd,stroke:#4a90d9\n    classDef decoder fill:#fff3e0,stroke:#f5a623\n    classDef velocity fill:#d4edda,stroke:#28a745\n    classDef yaw fill:#f3e5f5,stroke:#9c27b0\n\n    class A,B,C encoder\n    class D,E,F decoder\n    class G,H velocity\n    class I,J yaw</code></pre>"},{"location":"appendix-tensor-shapes/","title":"Appendix A: Tensor Shape Reference","text":"<p>00 Overview | 01 Data Pipeline | 02 Camera Branch | 03 LiDAR Branch | 04 Encoder Fusion | 05 Decoder Fusion | 06 Decoder | 07 Detection Heads | 08 Loss &amp; Training | 09 Inference | Appendix A: Tensors | Appendix B: Files</p> <p>Complete tensor shape reference through the full pipeline. Shapes assume <code>B=1, bev_h=bev_w=100, C=256, Ncam=6, num_query=450</code>.</p>"},{"location":"appendix-tensor-shapes/#input-stage","title":"Input Stage","text":"Tensor Shape Description <code>img</code> (train) <code>(B, T=4, 6, 3, H, W)</code> Stacked temporal multi-view images <code>img</code> (test) <code>(B, 6, 3, H, W)</code> Single-frame multi-view images <code>points</code> <code>list[(N_i, 4)]</code> Raw LiDAR point clouds per sample <code>can_bus</code> <code>(18,)</code> Ego-motion vector per frame"},{"location":"appendix-tensor-shapes/#camera-branch","title":"Camera Branch","text":"Tensor Shape Description ResNet stage outputs <code>(B, 6, C_l, H_l, W_l)</code> 4 levels: C = 256, 512, 1024, 2048 FPN outputs (<code>mlvl_feats[l]</code>) <code>(B, 6, 256, H_l, W_l)</code> 4 levels, all 256 channels <code>feat_flatten</code> <code>(6, sum_HW, B, 256)</code> Flattened + annotated with camera/level embeddings <code>spatial_shapes</code> <code>(4, 2)</code> H, W per FPN level <code>level_start_index</code> <code>(4,)</code> Cumulative start indices per level"},{"location":"appendix-tensor-shapes/#lidar-branch","title":"LiDAR Branch","text":"Tensor Shape Description <code>voxels</code> <code>(total_pillars, 20, 4)</code> Voxelized point clouds <code>coors</code> <code>(total_pillars, 4)</code> Pillar coordinates [batch, z, y, x] <code>num_points</code> <code>(total_pillars,)</code> Valid points per pillar Pillar features <code>(total_pillars, 64)</code> After PillarFeatureNet <code>bev_lidar</code> <code>(B, 64, 512, 512)</code> After PointPillarsScatter"},{"location":"appendix-tensor-shapes/#encoder","title":"Encoder","text":"Tensor Shape Description <code>bev_queries</code> <code>(B, 10000, 256)</code> Learned BEV query embeddings <code>bev_pos</code> <code>(B, 10000, 256)</code> BEV positional encoding <code>prev_bev</code> <code>(B, 10000, 256)</code> or <code>None</code> Previous frame BEV (frozen) <code>lidar_bev_tokens</code> (encoder) <code>(B, 10000, 256)</code> Projected LiDAR tokens for encoder-side fusion <code>ref_3d</code> <code>(B, 4, 10000, 3)</code> 3D reference points (4 pillar heights) <code>ref_2d</code> <code>(B, 10000, 1, 2)</code> 2D BEV grid reference points <code>reference_points_cam</code> <code>(6, B, 10000, 4, 2)</code> Per-camera 2D projections <code>bev_mask</code> <code>(6, B, 10000, 4)</code> Per-camera visibility mask TSA <code>q_cat</code> <code>(B, 10000, 512)</code> Concatenated [prev, cur] for offset prediction TSA <code>value</code> <code>(B*2, 10000, 256)</code> Stacked temporal frames SCA <code>queries_rebatch</code> <code>(B, 6, max_vis, 256)</code> Sparse per-camera queries Camera SCA output <code>(B, 10000, 256)</code> After scatter-add + average LiDAR SCA output <code>(B, 10000, 256)</code> After deformable attention Encoder output (<code>bev_embed</code>) <code>(B, 10000, 256)</code> Final BEV embedding"},{"location":"appendix-tensor-shapes/#decoder-side-fusion","title":"Decoder-Side Fusion","text":"Tensor Shape Description <code>bev_embed_cam</code> <code>(B, 10000, 256)</code> Cloned before LiDAR fusion (for velocity head) <code>bev_lidar_tok</code> (decoder) <code>(B, 10000, 256)</code> Projected + normalized LiDAR tokens Concatenated <code>(B, 10000, 512)</code> [camera BEV, LiDAR tokens] Fused <code>bev_embed</code> <code>(B, 10000, 256)</code> After Linear(512, 256) + LayerNorm"},{"location":"appendix-tensor-shapes/#decoder","title":"Decoder","text":"Tensor Shape Description Object queries <code>(B, 450, 256)</code> Learned query embeddings Query positional <code>(B, 450, 256)</code> Positional part of query_embed Initial reference points <code>(B, 450, 3)</code> Predicted via Linear + sigmoid Decoder memory (value) <code>(10000, B, 256)</code> Fused BEV (permuted) <code>inter_states</code> <code>(6, B, 450, 256)</code> Query features at each layer <code>inter_references</code> <code>(6, B, 450, 3)</code> Refined reference points at each layer"},{"location":"appendix-tensor-shapes/#detection-heads","title":"Detection Heads","text":"Tensor Shape Description <code>all_cls_scores</code> <code>(6, B, 450, 10)</code> Classification logits per layer <code>all_bbox_preds</code> <code>(6, B, 450, 10)</code> 10-dim bbox code per layer <code>all_yaw_bin_logits</code> <code>(6, B, 450, 24)</code> Yaw bin logits per layer <code>all_yaw_res_preds</code> <code>(6, B, 450, 2)</code> Yaw (sin, cos) residual per layer <code>all_vel_preds</code> <code>(6, B, 450, 2)</code> Velocity (vx, vy) per layer Velocity context <code>(B, 450, 256)</code> After cross-attention to camera-only BEV"},{"location":"appendix-tensor-shapes/#inference-output","title":"Inference Output","text":"Tensor Shape Description Selected scores <code>(K,)</code> Top-K sigmoid scores (K &lt;= 300) Selected labels <code>(K,)</code> Class labels Selected bboxes <code>(K, 9)</code> Denormalized: cx, cy, cz, w, l, h, yaw, vx, vy <p>Next: Appendix B - File Map &amp; Class Hierarchy</p>"}]}